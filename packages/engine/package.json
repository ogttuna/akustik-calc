{
  "name": "@dynecho/engine",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm,cjs --dts --clean",
    "lint": "eslint src --ext .ts",
    "test": "vitest run --maxWorkers=1",
    "typecheck": "tsc --project tsconfig.json"
  },
  "dependencies": {
    "@dynecho/catalogs": "workspace:*",
    "@dynecho/shared": "workspace:*"
  }
}
