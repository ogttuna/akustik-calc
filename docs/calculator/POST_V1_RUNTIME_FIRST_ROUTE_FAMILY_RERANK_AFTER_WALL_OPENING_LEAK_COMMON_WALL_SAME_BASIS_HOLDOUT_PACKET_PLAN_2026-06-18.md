# Post-V1 Runtime-First Route-Family Rerank After Wall Opening/Leak Common-Wall Same-Basis Holdout Packet - 2026-06-18

## Purpose

This is the selected next bounded rerank after the wall opening/leak
common-wall same-basis holdout packet. The packet found no accepted
same-family, same-basis, rights-safe holdout row for the current
opening/leak or common-wall field/building residual lane, so a runtime
owner is not authorized yet.

The calculator goal is to choose the next highest-ROI calculator slice
without drifting into source crawling, UI/report work, confidence copy,
or process cleanup. Runtime work is allowed only if the candidate has an
owned formula/anchor route, route-required physical inputs, and metric
basis integrity.

## Previous Packet

Previous selected action:

`post_v1_wall_opening_leak_common_wall_same_basis_holdout_packet_plan`

Previous selected file:

`packages/engine/src/post-v1-wall-opening-leak-common-wall-same-basis-holdout-packet-contract.test.ts`

Previous selected status:

`post_v1_wall_opening_leak_common_wall_same_basis_holdout_packet_landed_no_runtime_selected_runtime_first_route_family_rerank`

Selected candidate closed:

`wall.opening_leak_common_wall_same_basis_holdout_packet`

The packet reviewed one bounded evidence packet and kept
`acceptedSameBasisHoldoutRows: 0`, `runtimeValuesMoved 0`,
`runtimeFormulaRetunes: 0`, `sourceRowsImported: 0`, and
`frontendImplementationFilesTouched: 0`. The existing opening/leak and
common-wall runtime values remain benchmark/pin evidence only until an
admissible holdout exists.

## Selected Next

Selected next action:

`post_v1_runtime_first_route_family_rerank_after_wall_opening_leak_common_wall_same_basis_holdout_packet_plan`

Selected next file:

`packages/engine/src/post-v1-runtime-first-route-family-rerank-after-wall-opening-leak-common-wall-same-basis-holdout-packet-contract.test.ts`

Selected next plan:

`docs/calculator/POST_V1_RUNTIME_FIRST_ROUTE_FAMILY_RERANK_AFTER_WALL_OPENING_LEAK_COMMON_WALL_SAME_BASIS_HOLDOUT_PACKET_PLAN_2026-06-18.md`

Selected next label:

`post-V1 runtime-first route-family rerank after wall opening/leak common-wall same-basis holdout packet`

## Outcome

Landed status:

`post_v1_runtime_first_route_family_rerank_after_wall_opening_leak_common_wall_same_basis_holdout_packet_landed_no_runtime_selected_project_user_measured_wall_rw_anchor_schema_fingerprint`

Selected candidate:

`project_user_measured_wall_rw_anchor_schema_fingerprint`

The rerank found that the opening/leak common-wall packet had
`acceptedSameBasisHoldoutRows: 0`, so no opening/leak or common-wall
runtime owner is authorized. It subtracted closed framed-wall,
steel/open-web, and double-leaf revalidation chains, rejected UI-first
and broad source-crawl work, and selected the project/user measured
wall `Rw` anchor schema + fingerprint prerequisite.

Counters: `candidateCount: 9`, `roiAnalysisIterations: 4`,
`estimatedNextExactMeasuredAnchorInputFamilies: 1`,
`estimatedNextSharedDomainFilesTouched: 1`,
`estimatedNextRuntimeValuesMovedAfterBridge: 1`,
`runtimeValuesMoved 0`, `runtimeFormulaRetunes: 0`,
`sourceRowsImported: 0`, and `frontendImplementationFilesTouched: 0`.

Current selected next action:

`post_v1_project_user_measured_wall_rw_anchor_schema_fingerprint_plan`

Current selected next file:

`packages/shared/src/domain/project-user-measured-source-anchor.test.ts`

Current selected next plan:

`docs/calculator/POST_V1_PROJECT_USER_MEASURED_WALL_RW_ANCHOR_SCHEMA_FINGERPRINT_PLAN_2026-06-18.md`

Current selected next label:

`post-V1 project/user measured wall Rw anchor schema + fingerprint`

## Rerank Rules

1. Subtract closed chains first: user-material wall/floor companion
   completeness, timber-stud/CLT field/building lab companions, and the
   opening/leak common-wall holdout packet just closed here.
2. Prefer a runtime owner only if local implementation already proves an
   owned physics/formula route, required physical inputs, basis
   separation, and bounded target outputs.
3. Prefer a no-runtime coverage refresh when a recently moved runtime
   route needs target-output independence or negative-boundary
   protection.
4. Prefer a narrow evidence packet only when a named family is blocked
   by same-basis source/holdout absence and the missing evidence can be
   stated precisely.
5. Reject broad source crawling, UI/report work, docs-only cleanup,
   confidence-label work, and formula retunes without measured or
   compatible-anchor evidence.

## Candidate Families To Compare

- user-material physical input coverage for custom wall/floor stacks;
- building prediction and flanking runtime where room, area, and
  junction inputs are explicit;
- owned frequency-band backbone slices with visible companion metrics;
- companion metric completeness only when a spectrum or rating basis is
  already owned;
- narrow calibration/holdout packets for one named route family;
- residual families such as opening/leak, common wall, open-box, or
  open-web only when the previous blocker has new evidence or a new
  owner boundary.

## Validation

For this next no-runtime rerank:

1. targeted Vitest for the new rerank contract;
2. targeted Vitest for the landed holdout packet contract;
3. `git diff --check`.

If the rerank selects a runtime owner, the following owner turn must run
the selected owner test, adjacent route runtime/boundary tests,
`pnpm calculator:gate:current`, and `git diff --check`.
