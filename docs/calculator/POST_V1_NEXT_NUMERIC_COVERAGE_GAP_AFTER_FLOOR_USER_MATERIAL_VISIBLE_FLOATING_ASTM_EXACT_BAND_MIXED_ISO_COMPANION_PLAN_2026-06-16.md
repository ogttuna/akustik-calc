# Post-V1 Next Numeric Coverage Gap After Floor User-Material Visible Floating ASTM Exact-Band Mixed ISO Companion Plan - 2026-06-16

## North Star

DynEcho is an acoustic calculator. This rerank should pick the next
highest-ROI calculator slice after the custom visible heavy
floating-floor exact ASTM plus ISO companion route is protected by a
coverage refresh.

The selected follow-up must increase calculable combinations, improve
numeric accuracy, open an owned formula route, capture required physical
inputs, or protect metric/basis integrity. It must not become UI polish,
document cleanup, broad source crawling, source rows for their own sake,
or formula retuning without evidence.

## Previous Runtime Owner

Runtime owner action:

`post_v1_floor_user_material_visible_floating_astm_exact_band_mixed_iso_companion_owner_plan`

Runtime owner file:

`packages/engine/src/post-v1-floor-user-material-visible-floating-astm-exact-band-mixed-iso-companion-owner-contract.test.ts`

Runtime owner status:

`post_v1_floor_user_material_visible_floating_astm_exact_band_mixed_iso_companion_owner_landed_runtime_selected_coverage_refresh`

Selected candidate:

`floor.user_material_visible_floating_astm_exact_band_mixed_iso_companion_owner`

The owner preserves exact ASTM `IIC` / `AIIC`, carries same-stack ISO
`Ln,w 50.1` and `DeltaLw 24.4`, and keeps heavy airborne companions
`Rw 58`, `STC 57`, `C -1.8`, and `Ctr -7.3`. Counters:
`newCalculableLayerTemplates: 0`, `newCalculableRequestShapes: 2`,
`newCalculableTargetOutputs: 12`, `runtimeBasisPromotions: 2`,
`runtimeValuesMoved 12`, `runtimeFormulaRetunes: 0`,
`sourceRowsImported: 0`, and `frontendImplementationFilesTouched: 0`.
This is not a broad source crawl.

## Previous Coverage Refresh

Coverage refresh action:

`post_v1_floor_user_material_visible_floating_astm_exact_band_mixed_iso_companion_coverage_refresh_plan`

Coverage refresh file:

`packages/engine/src/post-v1-floor-user-material-visible-floating-astm-exact-band-mixed-iso-companion-coverage-refresh-contract.test.ts`

Coverage refresh status:

`post_v1_floor_user_material_visible_floating_astm_exact_band_mixed_iso_companion_coverage_refresh_landed_no_runtime_selected_next_numeric_coverage_gap`

The refresh re-probes exact ASTM `IIC 50` and `AIIC 50`, same-stack ISO
`Ln,w 50.1` and `DeltaLw 24.4`, and heavy airborne companions `Rw 58`,
`STC 57`, `C -1.8`, and `Ctr -7.3`. It keeps generic `IIC` / `AIIC`,
non-ASTM exact methods, missing dynamic stiffness, and missing load
basis outside the mixed owner. Counters:
`coverageRefreshContractFilesTouched: 1`,
`newCalculableLayerTemplates: 0`, `newCalculableRequestShapes: 0`,
`newCalculableTargetOutputs: 0`, `runtimeBasisPromotions: 0`,
`runtimeValuesMoved 0`, `runtimeFormulaRetunes: 0`,
`sourceRowsImported: 0`, and `frontendImplementationFilesTouched: 0`.
This is not a broad source crawl.

## Selected Next

Current selected next action:

`post_v1_next_numeric_coverage_gap_after_floor_user_material_visible_floating_astm_exact_band_mixed_iso_companion_plan`

Current selected next file:

`packages/engine/src/post-v1-next-numeric-coverage-gap-after-floor-user-material-visible-floating-astm-exact-band-mixed-iso-companion-contract.test.ts`

Current selected next plan doc:

`docs/calculator/POST_V1_NEXT_NUMERIC_COVERAGE_GAP_AFTER_FLOOR_USER_MATERIAL_VISIBLE_FLOATING_ASTM_EXACT_BAND_MIXED_ISO_COMPANION_PLAN_2026-06-16.md`

Current selected next label:

`post-V1 next numeric coverage gap after floor user-material visible floating ASTM exact-band mixed ISO companion`

## Closeout

Rerank status:

`post_v1_next_numeric_coverage_gap_after_floor_user_material_visible_floating_astm_exact_band_mixed_iso_companion_landed_no_runtime_selected_floor_user_material_visible_floating_astm_exact_band_field_impact_companion_owner`

Selected candidate:

`floor.user_material_visible_floating_astm_exact_band_field_impact_companion_owner`

The rerank ran `roiAnalysisIterations: 4` and `candidateCount: 10`.
It subtracts closed visible floating load-basis, mixed lab-companion,
heavy airborne companion, exact ASTM mixed ISO companion owner, and the
selected coverage refresh. It selects the exact ASTM field impact
companion owner because complete field context currently exposes a real
runtime exception: the field adapter changes the top-level impact basis
while exact ASTM `IIC` / `AIIC` remains present. The next owner should
move six field companion values without weakening ASTM/ISO metric-basis
integrity. Counters: `estimatedNextRuntimeBasisPromotions: 2`,
`estimatedNextRuntimeValuesMoved: 6`,
`estimatedNextCalculableRequestShapes: 2`,
`estimatedNextCalculableTargetOutputs: 6`, `runtimeValuesMoved 0`,
`runtimeFormulaRetunes: 0`, `sourceRowsImported: 0`, and
`frontendImplementationFilesTouched: 0`. This is not a broad source
crawl.

Current selected next action:

`post_v1_floor_user_material_visible_floating_astm_exact_band_field_impact_companion_owner_plan`

Current selected next file:

`packages/engine/src/post-v1-floor-user-material-visible-floating-astm-exact-band-field-impact-companion-owner-contract.test.ts`

Current selected next plan doc:

`docs/calculator/POST_V1_FLOOR_USER_MATERIAL_VISIBLE_FLOATING_ASTM_EXACT_BAND_FIELD_IMPACT_COMPANION_OWNER_PLAN_2026-06-16.md`

Current selected next label:

`post-V1 floor user-material visible floating ASTM exact-band field impact companion owner`

## Rerank Scope

The rerank should run after the coverage refresh lands. It should
subtract closed lanes:

- custom visible heavy floating-floor load-basis derivation;
- custom visible heavy floating-floor mixed lab companions;
- custom visible heavy floating-floor heavy airborne companion;
- exact ASTM `IIC` / `AIIC` mixed ISO companion owner;
- already-protected generic `unsupported` and `needs_input` boundaries.

Candidate comparison should prioritize:

- request shapes that unlock more numeric outputs for user-entered
  layers;
- owned formulas that need only explicit physical inputs already present
  in request/context surfaces;
- metric/basis owners that prevent aliasing while expanding outputs;
- boundaries whose protection prevents wrong values from being returned.

Candidate comparison must reject:

- broad source crawling without an identified formula/anchor route;
- UI-only work;
- formula retuning without measured or benchmark evidence;
- ASTM/ISO metric aliasing;
- CI, CI50, or field defaults without explicit user/source inputs;
- building-prediction impact promotion without owned field inputs.

Expected rerank counters:

- `runtimeValuesMoved 0`;
- `runtimeFormulaRetunes: 0`;
- `sourceRowsImported: 0`;
- `frontendImplementationFilesTouched: 0`.

The rerank may estimate the next runtime movement, but it should not
move runtime behavior itself.

## Validation

Run targeted Vitest:

```bash
pnpm --filter @dynecho/engine exec vitest run \
  src/post-v1-floor-user-material-visible-floating-astm-exact-band-mixed-iso-companion-coverage-refresh-contract.test.ts \
  src/post-v1-next-numeric-coverage-gap-after-floor-user-material-visible-floating-astm-exact-band-mixed-iso-companion-contract.test.ts
```

Before handoff, run:

```bash
pnpm calculator:gate:current
git diff --check
```
