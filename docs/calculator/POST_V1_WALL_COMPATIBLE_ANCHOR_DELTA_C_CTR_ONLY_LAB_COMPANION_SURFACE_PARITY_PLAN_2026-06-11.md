# Post-V1 Wall Compatible Anchor-Delta C/Ctr-Only Lab Companion Surface Parity Plan - 2026-06-11

Document role: selected no-runtime surface parity follow-up after the
C/Ctr-only lab companion owner. This is not a source crawl, UI polish
pass, or formula retune. It exists to prove the workbench/API/replay/report
surfaces carry the newly calculable C-only and Ctr-only outputs without
weakening metric-basis boundaries.

Plan doc path:
`docs/calculator/POST_V1_WALL_COMPATIBLE_ANCHOR_DELTA_C_CTR_ONLY_LAB_COMPANION_SURFACE_PARITY_PLAN_2026-06-11.md`.

## Dependency

Previous coverage refresh:
`post_v1_wall_compatible_anchor_delta_stc_only_lab_companion_coverage_refresh_plan`.

Previous coverage refresh file:
`packages/engine/src/post-v1-wall-compatible-anchor-delta-stc-only-lab-companion-coverage-refresh-contract.test.ts`.

Previous coverage refresh status:
`post_v1_wall_compatible_anchor_delta_stc_only_lab_companion_coverage_refresh_landed_no_runtime_selected_c_ctr_only_lab_companion_owner`.

Runtime owner:
`post_v1_wall_compatible_anchor_delta_c_ctr_only_lab_companion_owner_plan`.

Runtime owner file:
`packages/engine/src/post-v1-wall-compatible-anchor-delta-c-ctr-only-lab-companion-owner-contract.test.ts`.

Runtime owner status:
`post_v1_wall_compatible_anchor_delta_c_ctr_only_lab_companion_owner_landed_runtime_selected_surface_parity`.

The owner opens only standalone C-only and Ctr-only element-lab requests
for the Knauf `416889` compatible anchor-delta route:

- paired C-only: `C -1.1`;
- paired Ctr-only: `Ctr -6`;
- one-side C-only: `C -0.6`;
- one-side Ctr-only: `Ctr -5.5`.

This runtime owner is value-moving. These are calculated lab companion values from
`wall.compatible_anchor_delta.calculated_lab_companions` on runtime basis
`post_v1_wall_compatible_anchor_delta_calculated_lab_companion_runtime`.
They are not measured C/Ctr source evidence.

Owner counters: `newCalculableRequestShapes: 4`,
`newCalculableTargetOutputs: 2`, `runtimeBasisPromotions: 1`,
`runtimeValuesMoved 0`, `runtimeFormulaRetunes: 0`,
`sourceRowsImported: 0`, and `frontendImplementationFilesTouched: 0`.

## Selected Next

Selected next action:
`post_v1_wall_compatible_anchor_delta_c_ctr_only_lab_companion_surface_parity_plan`.

Selected next file:
`apps/web/features/workbench/post-v1-wall-compatible-anchor-delta-c-ctr-only-lab-companion-surface-parity.test.ts`.

Selected next label:
`post-V1 wall compatible anchor-delta C/Ctr-only lab companion surface parity`.

Status:
`post_v1_wall_compatible_anchor_delta_c_ctr_only_lab_companion_surface_parity_landed_no_runtime_selected_coverage_refresh`.

## Iteration 1 - Surface Scope

The surface parity should prove live calculation, calculator API payloads,
saved replay, server snapshot replay, output cards, target-output status,
and report summaries all preserve:

- paired `C -1.1`;
- paired `Ctr -6`;
- one-side `C -0.6`;
- one-side `Ctr -5.5`;
- selected candidate
  `wall.compatible_anchor_delta.calculated_lab_companions`;
- runtime basis
  `post_v1_wall_compatible_anchor_delta_calculated_lab_companion_runtime`.

## Iteration 2 - Boundaries

The surface must not imply a broader route than the owner landed:

- direct single `Rw` stays on the direct compatible anchor-delta owner;
- STC-only stays on the STC-only companion owner;
- mixed `Rw+STC/C/Ctr` stays on the existing lab companion route;
- `C+Ctr` without `Rw` or `STC` remains unsupported until explicitly
  selected;
- field/building, A-weighted, ASTM/IIC/AIIC, missing-input, and
  non-Knauf requests remain outside this owner.

## Iteration 3 - Expected Closeout

After surface parity lands, the next closeout should be a no-runtime
coverage refresh for this owner, not a broad source crawl. Expected
surface counters: `webSurfaceParityContractFilesTouched: 1`,
`frontendImplementationFilesTouched: 0`, `runtimeValuesMoved 0`,
`runtimeFormulaRetunes: 0`, and `sourceRowsImported: 0`.

## Closeout

The surface parity has landed no-runtime. Workbench live calculation,
calculator API payloads, saved replay, server snapshot replay, output
cards, target-output status, and report summaries now keep paired
`C -1.1`, paired `Ctr -6`, one-side `C -0.6`, and one-side `Ctr -5.5`
aligned through `wall.compatible_anchor_delta.calculated_lab_companions`
on runtime basis
`post_v1_wall_compatible_anchor_delta_calculated_lab_companion_runtime`.
Direct `Rw`, STC-only, mixed `Rw+STC/C/Ctr`, C+Ctr without `Rw`/`STC`,
field/building, A-weighted, ASTM/IIC/AIIC, missing-input, and non-Knauf
boundaries remain pinned. This is not a broad source crawl.

Counters: `webSurfaceParityContractFilesTouched: 1`,
`frontendImplementationFilesTouched: 0`, `runtimeValuesMoved 0`,
`runtimeFormulaRetunes: 0`, and `sourceRowsImported: 0`.

Selected next action:
`post_v1_wall_compatible_anchor_delta_c_ctr_only_lab_companion_coverage_refresh_plan`.

Selected next file:
`packages/engine/src/post-v1-wall-compatible-anchor-delta-c-ctr-only-lab-companion-coverage-refresh-contract.test.ts`.

Selected next plan doc:
`docs/calculator/POST_V1_WALL_COMPATIBLE_ANCHOR_DELTA_C_CTR_ONLY_LAB_COMPANION_COVERAGE_REFRESH_PLAN_2026-06-11.md`.

Selected next label:
`post-V1 wall compatible anchor-delta C/Ctr-only lab companion coverage refresh`.
