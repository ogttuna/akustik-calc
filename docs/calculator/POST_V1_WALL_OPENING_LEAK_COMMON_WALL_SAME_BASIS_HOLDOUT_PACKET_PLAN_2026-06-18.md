# Post-V1 Wall Opening/Leak Common-Wall Same-Basis Holdout Packet - 2026-06-18

## Purpose

This is the selected next bounded evidence packet after the runtime-first
rerank following wall timber-stud + CLT formula building lab-companion
basis integrity. It is not a broad source crawl and it does not move
runtime values by itself.

The calculator reason is concrete: opening/leak field and building
adapters already exist for explicit room, area, opening/leak, and
junction inputs, but common-wall residual ownership still needs
same-family, same-basis evidence before any runtime formula retune,
anchor delta, or building prediction widening is safe.

## Previous Rerank

Previous selected rerank action:

`post_v1_runtime_first_route_family_rerank_after_wall_timber_stud_clt_formula_building_lab_companion_basis_integrity_plan`

Previous selected rerank file:

`packages/engine/src/post-v1-runtime-first-route-family-rerank-after-wall-timber-stud-clt-formula-building-lab-companion-basis-integrity-contract.test.ts`

Previous selected rerank status:

`post_v1_runtime_first_route_family_rerank_after_wall_timber_stud_clt_formula_building_lab_companion_basis_integrity_landed_no_runtime_selected_wall_opening_leak_common_wall_same_basis_holdout_packet`

Selected candidate:

`wall.opening_leak_common_wall_same_basis_holdout_packet`

The rerank moved no runtime values, imported no source rows, and touched
no frontend implementation files. It selected this packet because the
immediate runtime candidates either repeated closed user-material,
opening/leak, open-box/open-web, and companion chains, or needed
source/holdout proof before promotion.

## Selected Next

Selected next action:

`post_v1_wall_opening_leak_common_wall_same_basis_holdout_packet_plan`

Selected next file:

`packages/engine/src/post-v1-wall-opening-leak-common-wall-same-basis-holdout-packet-contract.test.ts`

Selected next plan:

`docs/calculator/POST_V1_WALL_OPENING_LEAK_COMMON_WALL_SAME_BASIS_HOLDOUT_PACKET_PLAN_2026-06-18.md`

Selected next label:

`post-V1 wall opening/leak common-wall same-basis holdout packet`

## Packet Outcome

Status:

`post_v1_wall_opening_leak_common_wall_same_basis_holdout_packet_landed_no_runtime_selected_runtime_first_route_family_rerank`

The executable packet landed in:

`packages/engine/src/post-v1-wall-opening-leak-common-wall-same-basis-holdout-packet-contract.test.ts`

It reviewed the current local evidence and found
`acceptedSameBasisHoldoutRows: 0`. The current opening/leak field,
opening/leak building, A-weighted, and common-wall building values stay
benchmark-only runtime pins because they are source-absent formula
outputs, not measured same-basis holdouts. The Gate S lab opening/leak
anchor and common-wall lab/field family evidence remain wrong-basis or
missing-input context for the current building-prediction residual lane.

Counters: `evidencePacketsReviewed: 1`,
`localCandidateRowsReviewed: 7`, `benchmarkOnlyRows: 4`,
`rejectedCandidateRows: 3`, `acceptedSameBasisHoldoutRows: 0`,
`routeRequiredPhysicalInputsNamed: 11`, `runtimeValuesMoved 0`,
`runtimeFormulaRetunes: 0`, `sourceRowsImported: 0`, and
`frontendImplementationFilesTouched: 0`.

The packet defines the future acceptance gate: a rights-safe,
same-family, same-basis opening/leak or common-wall field/building
packet with route-required room, area, junction, flanking, and basis
inputs can become runtime-owner evidence, but no current local row
passes that gate.

Selected next action after this packet:

`post_v1_runtime_first_route_family_rerank_after_wall_opening_leak_common_wall_same_basis_holdout_packet_plan`

Selected next file after this packet:

`packages/engine/src/post-v1-runtime-first-route-family-rerank-after-wall-opening-leak-common-wall-same-basis-holdout-packet-contract.test.ts`

Selected next plan after this packet:

`docs/calculator/POST_V1_RUNTIME_FIRST_ROUTE_FAMILY_RERANK_AFTER_WALL_OPENING_LEAK_COMMON_WALL_SAME_BASIS_HOLDOUT_PACKET_PLAN_2026-06-18.md`

## Scope

The packet must answer whether a future runtime owner can safely promote
or calibrate a common-wall / opening-leak building prediction residual.
It should focus on one same-family packet, not a broad source crawl.

Target outputs under investigation:

- `R'w`;
- `Dn,w`;
- `Dn,A`;
- `DnT,w`;
- `DnT,A`.

Candidate physical inputs:

- `hostWallAreaM2`;
- `openingLeakElements`;
- `panelWidthMm`;
- `panelHeightMm`;
- `receivingRoomVolumeM3`;
- `receivingRoomRt60S`;
- `sourceRoomVolumeM3`;
- `flankingJunctionClass`;
- `junctionCouplingLengthM`;
- `buildingPredictionOutputBasis`.

Required evidence:

- same-family opening/leak or common-wall construction boundary;
- same metric basis for the selected target outputs;
- explicit distinction between measured exact row, compatible anchor,
  calibration holdout, and benchmark-only row;
- rights-safe metadata and storage posture;
- negative boundaries for near-miss rows and lab-only values.

## Implementation Steps

1. Create the selected contract test file:
   `packages/engine/src/post-v1-wall-opening-leak-common-wall-same-basis-holdout-packet-contract.test.ts`.
2. Encode packet acceptance criteria: family match, metric basis,
   construction boundary, rights status, route-required physical inputs,
   and whether the row can be exact, anchor, holdout, or benchmark only.
3. Re-probe local implementation evidence first:
   opening/leak lab runtime, opening/leak field/building runtime,
   A-weighted adapter, input surface, and common-wall residual ledgers.
4. Use internet research only if local evidence cannot establish a
   same-basis packet. Prefer standards, manufacturer technical data,
   papers, official software docs, or rights-safe source metadata.
5. Do not import source rows or retune formulas in this packet. The
   output of this slice is an executable evidence/readiness decision.
6. If a same-basis packet passes, select a separate runtime owner for
   the bounded residual formula, compatible anchor, or calibration
   budget. If no packet passes, keep runtime unsupported/needs-input and
   name the exact missing evidence.

## Boundaries

- Do not copy lab `Rw` into `R'w`, `Dn,w`, `Dn,A`, `DnT,w`, or
  `DnT,A`.
- Do not apply a near-miss source row to a different opening/leak or
  common-wall construction without a compatible-anchor rule.
- Do not use a confidence label to hide missing room, area, junction, or
  basis inputs.
- Do not substitute ASTM impact metrics for ISO airborne or building
  metrics.
- Do not broaden into a general source crawl or finite scenario pack.

## Expected Counters

- `evidencePacketsReviewed: 1`;
- `runtimeValuesMoved: 0`;
- `runtimeFormulaRetunes: 0`;
- `sourceRowsImported: 0`;
- `frontendImplementationFilesTouched: 0`;
- `estimatedRuntimeValuesMovedAfterEvidence: 5`.

## Validation

For this no-runtime packet:

1. targeted Vitest for the packet contract;
2. targeted Vitest for the previous rerank contract if the selected
   action docs move;
3. `git diff --check`.

For a following runtime owner, run the selected owner contract, adjacent
opening/leak field/building tests, `pnpm calculator:gate:current`, and
`git diff --check`.
