# Post-V1 Gate FC/FD Floor Raw-Bare Floating Holdout Plan - 2026-06-09

Status: Gate FC landed no-runtime and selected Gate FD. Gate FD has
since landed no-runtime, rejected the holdout owner, and selected Gate
FE.

Gate FC landed gate:
`post_v1_next_numeric_coverage_gap_gate_fc_plan`

Gate FC status:
`post_v1_next_numeric_coverage_gap_gate_fc_landed_no_runtime_selected_floor_raw_bare_and_floating_same_basis_holdout_gate_fd`

Gate FC selected candidate:
`floor.raw_bare_and_floating_same_basis_holdout_prerequisite_after_gate_fb`

Selected next action:
`post_v1_floor_raw_bare_and_floating_same_basis_holdout_gate_fd_plan`

Selected next file:
`packages/engine/src/post-v1-floor-raw-bare-and-floating-same-basis-holdout-gate-fd-contract.test.ts`

Gate FD landed status:
`post_v1_floor_raw_bare_and_floating_same_basis_holdout_gate_fd_landed_no_runtime_owner_rejected_selected_next_numeric_coverage_gap_gate_fe`

Gate FD owner decision:
`floor.raw_bare_and_floating.same_basis_holdout_owner_rejected_missing_admissible_holdouts`

Gate FD selected next action:
`post_v1_next_numeric_coverage_gap_gate_fe_plan`

Gate FD selected next file:
`packages/engine/src/post-v1-next-numeric-coverage-gap-gate-fe-contract.test.ts`

Gate FD closeout plan:
`docs/calculator/POST_V1_GATE_FD_FE_FLOOR_RAW_BARE_FLOATING_HOLDOUT_CLOSEOUT_PLAN_2026-06-09.md`

## Purpose

Gate FC reranks the current post-Gate-FB calculator gaps. Gate FB
rejected the opening/leak and common-wall same-basis residual owner, so
Gate FC must not keep spending runtime effort on that blocked wall
budget-tightening path.

No safe immediate value-moving runtime candidate remains from the
current evidence: opening/leak/common-wall residuals are rejected by Gate
FB, heavy-core lined-massive retune remains owner-rejected after Gate
EY/EW, and direct-fixed, reinforced-concrete visible-derived,
thick-board safety, ASTM exact-band, and steel visible input-surface
work are already closed.

The selected next action is therefore the smallest calculator-relevant
accuracy prerequisite: Gate FD must target the floor raw-bare and
floating residual ledgers already exposed by Gate CL. This is not a
broad source crawl and not a finite scenario library. It is a bounded
same-basis holdout proof needed before any raw-bare/floating floor
budget tightening or retune can be safe.

## Gate FC Iteration 1

Gate FC first subtracts Gate FB-blocked wall residual paths.

Rejected directions:

- opening/leak field/building/A-weighted budget tightening without
  source-owned same-basis holdouts;
- common flat double-leaf building-prediction budget tightening from lab
  or field rows;
- confidence wording, finite scenario packs, frontend polish, or broad
  catalog work as a substitute for calculator evidence.

Result: opening/leak/common-wall values and budgets stay frozen. Gate FC
must not move runtime values on that path.

## Gate FC Iteration 2

Gate FC then compares the remaining current implementation gaps.
Heavy-core / lined-massive retune remains owner-rejected. Already-closed
direct-fixed, reinforced-concrete visible-derived, thick-board safety,
ASTM exact-band, and steel visible input-surface work should not be
reopened.

The remaining bounded accuracy candidate is:
`floor.raw_bare_and_floating_same_basis_holdout_prerequisite_after_gate_fb`.

Gate FD is limited to these Gate CL residual ledgers:

- `floor.open_box_timber.raw_bare_lab_impact`
- `floor.open_web_steel.raw_bare_lab_impact`
- `floor.heavy_floating_upper_treatment.field_companion_gate_ch`

Each selected ledger currently has `holdoutRowIds: []`,
`budgetTighteningAdmitted: false`, `runtimePromotionAdmitted: false`,
and a wide budget (`9`, `9`, and `8` respectively). Gate FD may only
accept source-owned same-basis holdouts that match the ledger's route,
metric basis, layer family, and negative boundaries.

## Counters

- `candidateCount 10`
- `roiAnalysisIterations: 2`
- `blockedByGateFBOwnerRejectionRows 2`
- `blockedHeavyCoreOwnerRejectedRows 1`
- `closedRepeatRows 5`
- `estimatedNextTargetedHoldoutLedgers 3`
- `floorResidualLedgersSelected 3`
- `immediateRuntimeCandidatesSelected 0`
- `broadSourceCrawlSelected false`
- `newCalculableLayerTemplates 0`
- `newCalculableRequestShapes 0`
- `runtimeBasisPromotions 0`
- `runtimeFormulaRetunes 0`
- `runtimeValuesMoved 0`
- `sourceRowsImported: 0`
- `frontendImplementationFilesTouched: 0`

## Gate FD Work Order

Create:
`packages/engine/src/post-v1-floor-raw-bare-and-floating-same-basis-holdout-gate-fd-contract.test.ts`.

Gate FD must evaluate only the three selected floor residual ledgers.
It must produce one of two outcomes:

1. accept one or more source-owned same-basis holdout ledgers and select
   a later runtime/budget action; or
2. reject the owner because admissible holdouts are absent and select a
   fresh numeric gap rerank without moving values.

Gate FD must not import a broad floor catalog, borrow packaged rows into
raw-bare lanes, use `Ln,w` rows as `DeltaLw` owners without a valid
same-basis relationship, alias ISO impact metrics to ASTM `IIC`/`AIIC`,
or tighten budgets from source proximity alone.

## Gate FD Outcome

Gate FD chose outcome 2: the owner remains rejected because admissible
same-basis holdouts are absent. Open-box and open-web raw-bare formula
outputs remain source-absent, package/finished/supported-band evidence
is not a raw-bare same-basis holdout, and the Gate CH published `Ln,w`
anchor plus field adapter outputs are not measured direct+flanking field
holdouts. Runtime values and budgets remain frozen.

Gate FD counters: `ownerLedgersRejected 3`,
`admissibleHoldoutLedgers 0`, `evaluatedGateCLResidualLedgers 3`,
`rejectedCandidateEvidenceLedgers 6`, `boundaryLedgersPinned 7`,
`runtimeBudgetTighteningAdmitted 0`, `broadSourceCrawlSelected false`,
`newCalculableLayerTemplates 0`, `newCalculableRequestShapes 0`,
`runtimeBasisPromotions 0`, `runtimeFormulaRetunes 0`,
`runtimeValuesMoved 0`, `sourceRowsImported: 0`, and
`frontendImplementationFilesTouched: 0`.

Gate FD selected Gate FE:
`post_v1_next_numeric_coverage_gap_gate_fe_plan`
in
`packages/engine/src/post-v1-next-numeric-coverage-gap-gate-fe-contract.test.ts`.

## Stop Conditions

Stop Gate FD without runtime movement if any of these are true:

- the candidate row is not source-owned or cannot be traced;
- the row is not same-basis for the selected ledger;
- a packaged, finished, or supported-band row is being borrowed into a
  raw-bare ledger;
- lab impact evidence is being borrowed into field direct/flanking
  output without the required field context;
- the candidate only supports ASTM `IIC`/`AIIC` while the selected
  ledger is ISO `Ln,w`/`DeltaLw`/`CI`;
- negative boundary rows are missing or contradict the proposed owner;
- fewer holdouts exist than the selected owner requires.

## Validation Plan

- `pnpm -C packages/engine exec vitest run src/post-v1-next-numeric-coverage-gap-gate-fc-contract.test.ts --maxWorkers=1`
- `pnpm -C packages/engine exec vitest run src/post-v1-opening-leak-common-wall-same-basis-residual-owner-gate-fb-contract.test.ts src/post-v1-next-numeric-coverage-gap-gate-fc-contract.test.ts --maxWorkers=1`
- `pnpm calculator:gate:current`
- `git diff --check`
