# Post-V1 Runtime-First Route-Family Rerank After Wall Context-Owned Porous-Cavity Field/Building Lab-Companion Basis Integrity - 2026-06-19

## Purpose

This is the selected next no-runtime rerank after the wall
context-owned porous-cavity field/building lab-companion basis-integrity
coverage refresh. It must choose one calculator-first runtime owner or
explicitly stop on a physically necessary evidence/input blocker.

This plan does not authorize UI polish, confidence-copy work, broad
source crawling, generic cleanup, or source-row substitution. It exists
to select the next slice that improves calculator scope, accuracy,
route-required input capture, metric/basis integrity, or an owned
`needs_input` / `unsupported` boundary.

## Previous Coverage Refresh

Coverage refresh action:

`post_v1_wall_context_owned_porous_cavity_field_building_lab_companion_basis_integrity_coverage_refresh_plan`

Coverage refresh file:

`packages/engine/src/post-v1-wall-context-owned-porous-cavity-field-building-lab-companion-basis-integrity-coverage-refresh-contract.test.ts`

Coverage refresh plan:

`docs/calculator/POST_V1_WALL_CONTEXT_OWNED_POROUS_CAVITY_FIELD_BUILDING_LAB_COMPANION_BASIS_INTEGRITY_COVERAGE_REFRESH_PLAN_2026-06-19.md`

Coverage refresh status:

`post_v1_wall_context_owned_porous_cavity_field_building_lab_companion_basis_integrity_coverage_refresh_landed_no_runtime_selected_runtime_first_route_family_rerank`

The coverage refresh followed the previous runtime-first rerank:

`post_v1_runtime_first_route_family_rerank_after_project_user_measured_wall_airborne_frequency_field_building_lab_companion_target_output_independence_coverage_refresh_plan`

Previous rerank file:

`packages/engine/src/post-v1-runtime-first-route-family-rerank-after-project-user-measured-wall-airborne-frequency-field-building-lab-companion-target-output-independence-coverage-refresh-contract.test.ts`

Previous rerank status:

`post_v1_runtime_first_route_family_rerank_after_project_user_measured_wall_airborne_frequency_field_building_lab_companion_target_output_independence_coverage_refresh_landed_no_runtime_selected_wall_context_owned_porous_cavity_field_building_lab_companion_basis_integrity_owner`

Previous runtime owner:

`post_v1_wall_context_owned_porous_cavity_field_building_lab_companion_basis_integrity_owner_plan`

Owner file:

`packages/engine/src/post-v1-wall-context-owned-porous-cavity-field-building-lab-companion-basis-integrity-owner-contract.test.ts`

Owner status:

`post_v1_wall_context_owned_porous_cavity_field_building_lab_companion_basis_integrity_owner_landed_runtime_selected_coverage_refresh`

Selected candidate re-probed:

`wall.context_owned_porous_cavity_field_building_lab_companion_basis_integrity_owner`

The coverage refresh protects the context-owned physical-input
double-leaf/framed wall route: with explicit side leaf groups,
independent frame topology, absorptive cavity depth/fill, and
`advancedWall.cavities[].absorberFlowResistivityPaSM2`, field/building
lab companions stay on `Rw 46`, `STC 46`, `C -1`, and `Ctr -6.1`, while
field/building adapter outputs stay `R'w 40`, `Dn,w 41`, `Dn,A 39.5`,
`DnT,w 43`, and `DnT,A 41.9`. Missing physical inputs and impact
aliases remain outside.

Coverage counters: `coverageRefreshContractFilesTouched: 1`,
`newCalculableLayerTemplates: 0`, `newCalculableRequestShapes: 0`,
`newCalculableTargetOutputs: 0`, `runtimeBasisPromotions: 0`,
`runtimeValuesMoved 0`, `runtimeFormulaRetunes: 0`,
`sourceRowsImported: 0`, and `frontendImplementationFilesTouched: 0`.
This is not a broad source crawl.

## Rerank Candidate Streams

The rerank must compare at least these calculator streams:

- user-material physical input coverage: adjacent explicit topology,
  absorber, framing, dynamic-stiffness, and load-basis routes that can
  move real user-entered combinations without name shortcuts;
- building prediction and flanking runtime: narrow direct-plus-field or
  direct-plus-building routes with owned room, area, junction, or
  flanking context;
- frequency-band backbone: only a route that already owns or can safely
  return a one-third-octave curve without global rewrite;
- companion metric completeness: owned-spectrum/rating routes where
  `Rw`, `STC`, `C`, `Ctr`, `OITC`, `Ln,w`, `IIC`, or `AIIC` companions
  are hidden but derivable without scalar aliasing;
- calibration and holdout packets: one family with same-basis,
  rights-safe measured evidence and explicit before/after error budget;
- opening/leak, common-wall, open-box, or open-web residual families:
  only when the formula, exact row, compatible anchor, or required input
  surface is physically owned.

## Selection Gates

Before selecting an owner, the rerank contract must prove:

- the selected route names its owned formula, exact measured row,
  compatible anchor, spectrum owner, or adapter basis;
- all required physical inputs are explicit, or missing values return
  precise `needs_input`;
- the owner moves runtime values, target outputs, request shapes, or a
  reproduced wrong-basis/wrong-answer boundary;
- lab, field, building, ISO, ASTM, and A-weighted bases remain separate;
- adjacent unsupported routes stay unsupported.

## Expected Output

Rerank action:

`post_v1_runtime_first_route_family_rerank_after_wall_context_owned_porous_cavity_field_building_lab_companion_basis_integrity_plan`

Rerank plan:

`docs/calculator/POST_V1_RUNTIME_FIRST_ROUTE_FAMILY_RERANK_AFTER_WALL_CONTEXT_OWNED_POROUS_CAVITY_FIELD_BUILDING_LAB_COMPANION_BASIS_INTEGRITY_PLAN_2026-06-19.md`

The rerank contract should land at:

`packages/engine/src/post-v1-runtime-first-route-family-rerank-after-wall-context-owned-porous-cavity-field-building-lab-companion-basis-integrity-contract.test.ts`

It should select exactly one next owner action/file/plan, or document
why a physically necessary evidence/input prerequisite blocks safe
runtime movement. Prefer a runtime owner with measurable calculator
movement over another support-only step.
