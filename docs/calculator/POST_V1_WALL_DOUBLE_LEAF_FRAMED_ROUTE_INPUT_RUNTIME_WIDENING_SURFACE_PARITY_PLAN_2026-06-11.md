# Post-V1 Wall Double-Leaf Framed Route-Input Runtime Widening Surface Parity Plan - 2026-06-11

Document role: selected follow-up after
`post_v1_wall_double_leaf_framed_route_input_runtime_widening_plan`.
This is a calculator-surface parity slice, not a broad source crawl,
report polish, auth/storage work, or another no-runtime selection
ledger.

Selected next action:
`post_v1_wall_double_leaf_framed_route_input_runtime_widening_surface_parity_plan`.

Selected next file:
`apps/web/features/workbench/post-v1-wall-double-leaf-framed-route-input-runtime-widening-surface-parity.test.ts`.

Selected next label:
`post-V1 wall double-leaf/framed route-input runtime widening surface parity`.

Surface parity status:
`post_v1_wall_double_leaf_framed_route_input_runtime_widening_surface_parity_landed_no_runtime_selected_coverage_refresh`.

Selected follow-up action:
`post_v1_wall_double_leaf_framed_route_input_runtime_widening_coverage_refresh_plan`.

Selected follow-up file:
`packages/engine/src/post-v1-wall-double-leaf-framed-route-input-runtime-widening-coverage-refresh-contract.test.ts`.

Selected follow-up plan doc:
`docs/calculator/POST_V1_WALL_DOUBLE_LEAF_FRAMED_ROUTE_INPUT_RUNTIME_WIDENING_COVERAGE_REFRESH_PLAN_2026-06-11.md`.

Selected follow-up label:
`post-V1 wall double-leaf/framed route-input runtime widening coverage refresh`.

Previous runtime owner:
`post_v1_wall_double_leaf_framed_route_input_runtime_widening_plan`.

Previous runtime owner file:
`packages/engine/src/post-v1-wall-double-leaf-framed-route-input-runtime-widening-contract.test.ts`.

Previous runtime owner status:
`post_v1_wall_double_leaf_framed_route_input_runtime_widening_landed_runtime_selected_surface_parity`.

The runtime owner opens a context-only empty-cavity double-leaf/framed
wall request where the visible layers are two board leaves and the
route-required cavity is supplied as physical input:
`cavity1DepthMm`, `cavity1FillCoverage: empty`,
`cavity1AbsorptionClass: none`, side leaf groups, support topology, and
support spacing. The calculated element-lab outputs are `Rw 42`,
`STC 42`, `C -1`, and `Ctr -6.1` through runtime basis
`layer_combination_resolver_double_leaf_framed_wall_banded_source_absent_formula_corridor`.
Previous runtime counters: `newCalculableLayerTemplates: 1`,
`newCalculableRequestShapes: 1`, `newCalculableTargetOutputs: 4`,
`runtimeBasisPromotions: 1`, `frontendImplementationFilesTouched: 0`,
`runtimeValuesMoved 0`, `runtimeFormulaRetunes: 0`, and
`sourceRowsImported: 0`.

## Surface Scope

The surface parity contract should prove that the workbench and
calculator API preserve the same request shape and the same result
surface:

- live calculation accepts the two visible board leaves plus explicit
  empty-cavity route inputs;
- calculator API payloads preserve `cavity1DepthMm`, empty fill
  coverage, no-absorber classification, side leaf groups, support
  topology, and support spacing;
- saved replay and server snapshot replay reproduce `Rw 42`, `STC 42`,
  `C -1`, and `Ctr -6.1`;
- output cards, target-output status, and report summaries expose the
  runtime basis
  `layer_combination_resolver_double_leaf_framed_wall_banded_source_absent_formula_corridor`;
- context-only absorptive cavities without a visible porous-fill layer
  stay `needs_input` for `cavity1FillCoverage` and `absorberClass`;
- direct-fixed requests stay on the direct-fixed owner, exact/anchor
  rows still win, and ASTM/IIC/AIIC plus floor-impact outputs stay
  unsupported.

## Boundaries

This follow-up must not retune the double-leaf/framed formula, import
source rows, widen field/building adapters, or weaken the thick-board
Auto family guard. If the UI already serializes all required fields, the
slice should be a surface parity contract only. If a field is missing
from the surface, add only the minimum plumbing needed for this request
shape.

Expected counters for the follow-up: `webSurfaceParityContractFilesTouched: 1`,
`frontendImplementationFilesTouched: 0` unless payload plumbing is
required, `newCalculableLayerTemplates: 0`, `newCalculableRequestShapes: 0`,
`newCalculableTargetOutputs: 0`, `runtimeBasisPromotions: 0`,
`runtimeValuesMoved 0`, `runtimeFormulaRetunes: 0`, and
`sourceRowsImported: 0`.

## Surface Closeout

The surface parity has landed without runtime value movement. It proves
live workbench calculation, calculator API payloads, saved replay, server
snapshot replay, output cards, target-output status, and report
summaries preserve the context-only empty-cavity two-board result:
`Rw 42`, `STC 42`, `C -1`, and `Ctr -6.1` on runtime basis
`layer_combination_resolver_double_leaf_framed_wall_banded_source_absent_formula_corridor`.
It also keeps missing cavity classification on `needs_input` for
`cavity1FillCoverage` and `absorberClass`, and keeps field/building
aliases plus ASTM/IIC/AIIC outside this owner. Counters:
`webSurfaceParityContractFilesTouched: 1`,
`frontendImplementationFilesTouched: 0`, `newCalculableLayerTemplates: 0`,
`newCalculableRequestShapes: 0`, `newCalculableTargetOutputs: 0`,
`runtimeBasisPromotions: 0`, `runtimeValuesMoved 0`,
`runtimeFormulaRetunes: 0`, and `sourceRowsImported: 0`.
