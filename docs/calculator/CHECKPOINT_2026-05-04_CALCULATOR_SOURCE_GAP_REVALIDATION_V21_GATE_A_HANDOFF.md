# Checkpoint - 2026-05-04 - Calculator Source Gap Revalidation V21 Gate A

Slice id: `calculator_source_gap_revalidation_v21`

Status: LANDED / NO RUNTIME

Landed gate:

`gate_a_revalidate_source_gap_order_after_field_output_guard_and_company_internal_blocker`

Selection status:

`selected_company_internal_frequent_combination_lane_snapshot_guard_after_v21_consumed_field_output_guard_and_kept_rockwool_source_blocked`

Selected next slice:

`company_internal_frequent_combination_lane_snapshot_guard_v1`

Selected planning surface:

`docs/calculator/SLICE_COMPANY_INTERNAL_FREQUENT_COMBINATION_LANE_SNAPSHOT_GUARD_PLAN.md`

Selected first file:

`packages/engine/src/company-internal-frequent-combination-lane-snapshot-guard-gate-a-contract.test.ts`

Selected first action:

`gate_a_pin_company_internal_frequent_combination_lane_snapshot_matrix_no_runtime`

## What Landed

V21 consumed the field-output visible-basis guard and re-ranked the
source / accuracy backlog against the current company-internal blocker.
It found no source-ready runtime candidate and made no runtime, support,
confidence, evidence, API, route-card, output-card, proposal/report, or
workbench-input change.

The current split-rockwool / gypsum / MLV triple-leaf answer remains
screening only:

- grouped answer: `Rw 41`;
- route strategy: `multileaf_screening_blend`;
- Uris 2006 disposition: `paused_waiting_rights_safe_source_packet`;
- exact/source-validated status: not fixed, not exact, not
  source-validated.

V21 selected the next bounded no-runtime guard because company-internal
high-accuracy opening still needs a current matrix of frequent wall and
floor lane snapshots. That matrix must pin family, strategy, support,
confidence, source/origin, warnings, and visible posture for common
combinations before any company-internal high-accuracy handoff.

## Gate A Artifacts

`post_gate_b_source_gap_candidate_order`

The next highest-value bounded work is the company-internal frequent
combination lane snapshot guard. It is selected ahead of exact rockwool
runtime work because the rights-safe Uris 2006 or equivalent source
packet is still absent.

`rockwool_triple_leaf_fix_path_status`

Rockwool exact promotion remains blocked until all of these are owned:
rights-safe source-owned curves or numeric band vectors, curve identity
and rating derivation, uncertainty and tolerance owner, local rockwool /
gypsum / MLV / plaster mapping, grouped-topology runtime guard,
negative boundaries, and paired engine / web / route / output / report
tests.

`company_internal_high_accuracy_blocker_alignment`

The company-internal high-accuracy opening blocker remains active.
Frequent lane snapshots, source-promotion ownership, hostile API/import
guards, field-output ownership, and rockwool exact-or-screening-only
posture must stay green before rollout.

`field_output_guard_consumed_as_prerequisite`

Field-output visible copy has been guarded, but that guard is not a
field-output owner. `R'w`, `DnT,w`, `L'n,w`, and `L'nT,w` must still not
look design-grade unless a later source/field owner is introduced and
tested.

`frequent_combination_lane_snapshot_risk_order`

The selected snapshot guard must prioritize:

1. rockwool-like triple-leaf and flat-list swaps;
2. ordinary LSF / timber double-board layer swaps;
3. masonry / lined-massive boundary hybrids;
4. raw floor role inference and duplicate floor roles;
5. near-source material alias rows;
6. hostile API/import payloads.

`selected_next_slice_or_no_runtime_closeout`

Next slice:

`company_internal_frequent_combination_lane_snapshot_guard_v1`

Next file:

`packages/engine/src/company-internal-frequent-combination-lane-snapshot-guard-gate-a-contract.test.ts`

## Validation

Validation completed on 2026-05-04:

- focused V21 passed 1 file / 8 tests;
- engine continuity with company blocker, v20, Uris Gate U, and
  route/source risk register passed 5 files / 31 tests;
- web continuity with field-output Gate B, wall triple-leaf
  company-internal rehearsal, and flat-list multileaf route-card guard
  passed 3 files / 14 tests;
- `pnpm calculator:gate:current` passed after a transient Next build
  artifact retry: engine 242 files / 1396 tests, web 50 files / 238
  passed + 18 skipped, repo build 5 / 5 tasks, and whitespace guard
  green.

Known non-fatal build warnings remain the existing optional
`sharp/@img` warnings through `@turbodocx/html-to-docx`.
Runtime-visible calculator behavior stayed frozen.
