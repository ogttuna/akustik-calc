# Slice Plan - Rockwool Triple-Leaf Explicit Screening-Only Policy V1

Slice id: `rockwool_triple_leaf_explicit_screening_only_policy_v1`

Status: GATE C CLOSED / SELECTED SOURCE PROMOTION HOSTILE-INPUT READINESS GUARD

Selected by:

`calculator_source_gap_revalidation_v23` Gate A

Selection status:

`selected_rockwool_triple_leaf_explicit_screening_only_policy_after_v23_confirmed_uris_source_blocked_and_field_output_owner_closed`

Selected first file:

`packages/engine/src/rockwool-triple-leaf-explicit-screening-only-policy-gate-a-contract.test.ts`

Selected first action:

`gate_a_inventory_rockwool_triple_leaf_screening_only_policy_after_v23_rerank`

Prior checkpoint:

`docs/calculator/CHECKPOINT_2026-05-05_CALCULATOR_SOURCE_GAP_REVALIDATION_V23_GATE_A_HANDOFF.md`

Planning surface:

`docs/calculator/SLICE_ROCKWOOL_TRIPLE_LEAF_EXPLICIT_SCREENING_ONLY_POLICY_V1_PLAN.md`

Required carry-forward artifacts:

- `rockwool_uris_exact_runtime_still_blocked`
- `rockwool_explicit_screening_only_policy_selected`
- `source_promotion_and_hostile_input_ownership_carry_forward`
- `repeat_uris_acquisition_blocked_without_new_packet`
- `rockwool_triple_leaf_screening_surface_inventory`
- `grouped_rw41_and_flat_rw42_runtime_freeze`
- `visible_route_output_report_policy_gap`
- `rockwool_visible_gate_b_selected`
- `source_promotion_hostile_input_carry_forward`
- `pre_company_internal_use_exit_criteria`
- `visible_rockwool_screening_only_policy_guard`
- `rockwool_output_card_screening_only_copy`
- `rockwool_proposal_report_screening_only_copy`
- `rockwool_field_continuation_screening_bridge`
- `rockwool_non_target_boundary_copy_guard`
- `selected_gate_c_closeout_or_next_slice_with_target_file`
- `rockwool_policy_gate_c_closeout_summary`
- `rockwool_exact_or_screening_company_criterion_closed`
- `source_promotion_hostile_input_opening_blockers_carry_forward`
- `source_promotion_hostile_input_readiness_guard_selected`
- `selected_gate_a_source_promotion_hostile_input_readiness_with_target_file`

## Landed Gate C

Landed status:

`closed_rockwool_triple_leaf_explicit_screening_only_policy_no_runtime_and_selected_source_promotion_hostile_input_readiness_guard`

Landed file:

`packages/engine/src/post-rockwool-triple-leaf-explicit-screening-only-policy-v1-next-slice-selection-contract.test.ts`

Selected next slice:

`source_promotion_hostile_input_readiness_guard_v1`

Selected next file:

`packages/engine/src/source-promotion-hostile-input-readiness-guard-gate-a-contract.test.ts`

Selected next action:

`gate_a_inventory_source_promotion_and_hostile_input_readiness_after_rockwool_policy_closeout`

Selected planning surface:

`docs/calculator/SLICE_SOURCE_PROMOTION_HOSTILE_INPUT_READINESS_GUARD_V1_PLAN.md`

Gate C produced:

- `rockwool_policy_gate_c_closeout_summary`
- `rockwool_exact_or_screening_company_criterion_closed`
- `source_promotion_hostile_input_opening_blockers_carry_forward`
- `source_promotion_hostile_input_readiness_guard_selected`
- `selected_gate_a_source_promotion_hostile_input_readiness_with_target_file`

Gate C is no-runtime. Runtime values, support, confidence, evidence,
API behavior, route-card values, output-card status, proposal/report
values, and workbench-input behavior stayed frozen. The Rockwool
company-internal criterion is closed as explicit screening-only, not as
exact runtime. Source promotion ownership and hostile API/import
fail-closed proof remain active blockers before company-internal
high-accuracy opening.

## Landed Gate B

Landed status:

`gate_b_pinned_visible_rockwool_triple_leaf_screening_only_policy_no_runtime_selected_gate_c_closeout`

Landed file:

`apps/web/features/workbench/rockwool-triple-leaf-explicit-screening-only-policy-gate-b-visible.test.ts`

Selected next file:

`packages/engine/src/post-rockwool-triple-leaf-explicit-screening-only-policy-v1-next-slice-selection-contract.test.ts`

Selected next action:

`gate_c_no_runtime_closeout_and_next_slice_selection`

Gate B produced:

- `visible_rockwool_screening_only_policy_guard`
- `rockwool_output_card_screening_only_copy`
- `rockwool_proposal_report_screening_only_copy`
- `rockwool_field_continuation_screening_bridge`
- `rockwool_non_target_boundary_copy_guard`
- `selected_gate_c_closeout_or_next_slice_with_target_file`

Gate B is visible-copy only. Runtime values, support, confidence,
evidence, API behavior, route-card values, output-card status,
proposal/report values, and workbench-input behavior stayed frozen.
Grouped split-rockwool remains `Rw 41`, flat-list adjacent swaps remain
`Rw 42`, and field `R'w 34` / `DnT,w 36` remain continuations from the
Rockwool screening lane.

Gate B added the visible `Rockwool screening-only` label to output-card
and proposal/report coverage for the grouped/fail-closed Rockwool
multileaf lane. It also bridges field-output copy so finite field
outputs cannot look like independent measured field or design-grade
results. Non-target Rockwool double-leaf rows keep the existing generic
airborne screening copy.

## Landed Gate A

Landed status:

`gate_a_inventoried_rockwool_triple_leaf_screening_only_policy_no_runtime_selected_visible_gate_b`

Landed file:

`packages/engine/src/rockwool-triple-leaf-explicit-screening-only-policy-gate-a-contract.test.ts`

Selected next file:

`apps/web/features/workbench/rockwool-triple-leaf-explicit-screening-only-policy-gate-b-visible.test.ts`

Selected next action:

`gate_b_pin_visible_rockwool_triple_leaf_screening_only_policy`

Gate A produced:

- `rockwool_triple_leaf_screening_surface_inventory`
- `grouped_rw41_and_flat_rw42_runtime_freeze`
- `visible_route_output_report_policy_gap`
- `rockwool_visible_gate_b_selected`
- `source_promotion_hostile_input_carry_forward`
- `pre_company_internal_use_exit_criteria`

Gate A is no-runtime. Runtime values, support, confidence, evidence,
API behavior, route-card values, output-card status, proposal/report
values, and workbench-input behavior stayed frozen. Grouped
split-rockwool remains `Rw 41`, flat-list adjacent swaps remain `Rw 42`
fail-closed until grouped topology is supplied, and field `R'w 34` /
`DnT,w 36` remain continuations from the screening lane.

Gate A selected visible Gate B because output-card/report surfaces can
still show generic live/screening copy without a Rockwool-specific
screening-only label.

## Objective

Close the Rockwool side of the company-internal high-accuracy opening
criterion without pretending the calculation is fixed. If exact
Rockwool/Uris runtime promotion is still blocked, every engine,
route-card, output-card, proposal/report, and workbench-visible surface
must agree that grouped split-rockwool is screening-only, not exact,
not source-validated, and not suitable as a design-grade value.

This slice is not a value retune and is not source evidence. It must not
change the current `Rw 41` grouped result, the `Rw 42` flat-list
fail-closed swap result, support, confidence, evidence, API behavior,
route-card values, output-card status, proposal/report values, or
workbench-input behavior unless a later gate explicitly names and tests
a visible wording-only change.

## Inputs From V23

`rockwool_uris_exact_runtime_still_blocked`

Grouped split-rockwool remains `Rw 41`, `multileaf_screening_blend`,
low confidence, screening-only, not exact, and not source-validated.
Uris 2006 remains `paused_waiting_rights_safe_source_packet`.

`rockwool_explicit_screening_only_policy_selected`

Exact runtime is blocked, but private-use readiness needs the Rockwool
criterion to be either exact or explicitly screening-only. V23 selected
this bounded policy slice because it reduces misuse risk without
inventing source evidence.

`source_promotion_and_hostile_input_ownership_carry_forward`

Near-source aliases remain context only. Source promotion still requires
topology, material mapping, metric context, tolerance, negative
boundaries, and paired visible tests. Hostile API/import payloads must
remain fail-closed.

## Gate A Plan

1. Create
   `packages/engine/src/rockwool-triple-leaf-explicit-screening-only-policy-gate-a-contract.test.ts`.
2. Inventory engine and visible surfaces that currently expose Rockwool
   triple-leaf screening posture:
   - grouped topology engine trace and warnings;
   - flat-list adjacent-swap fail-closed route-card guard;
   - company-internal acceptance rehearsal;
   - output card and proposal/report wording surfaces;
   - docs/current-gate runner surfaces.
3. Prove no runtime value, support, confidence, evidence, API, route
   card value, output-card status, proposal/report value, or input
   behavior changes in Gate A.
4. Select visible Gate B only if a concrete wording gap exists. If all
   visible surfaces are already explicit enough, close no-runtime and
   keep the next correctness blocker in source-promotion or hostile-input
   ownership.

Gate A selected visible Gate B because output-card/report surfaces can
still show generic live/screening copy without a Rockwool-specific
screening-only label.

## Gate B Plan

Gate B landed visible-copy only and selected Gate C closeout.

1. Create
   `apps/web/features/workbench/rockwool-triple-leaf-explicit-screening-only-policy-gate-b-visible.test.ts`.
2. Pin visible output-card and proposal/report copy for grouped
   split-rockwool so `Rw 41` cannot look exact, source-validated, or
   design-grade.
3. Pin field continuation copy for `R'w 34` and `DnT,w 36` so they stay
   visible as continuations from the screening lane, not independent
   measured field results.
4. Keep runtime values, support, confidence, evidence, API behavior,
   route-card values, output-card status, proposal/report values, and
   workbench-input behavior frozen except for explicitly tested visible
   wording.
5. Select Gate C closeout / next-slice selection after visible wording
   is pinned.

## Gate C Plan

1. Create
   `packages/engine/src/post-rockwool-triple-leaf-explicit-screening-only-policy-v1-next-slice-selection-contract.test.ts`.
2. Close `rockwool_triple_leaf_explicit_screening_only_policy_v1`
   no-runtime with Gate A and Gate B artifacts carried forward.
3. Select the next bounded source/hostile-input readiness slice. Exact
   Rockwool runtime promotion remains blocked until a rights-safe
   Uris/source-owned curve packet and topology/material/metric/tolerance/
   negative-boundary/visible-test ownership exist.
4. Keep company-internal high-accuracy opening blocked until source
   promotion ownership, hostile API/import fail-closed proof,
   `pnpm calculator:gate:current`, and broad `pnpm check` are green at
   the opening handoff.

## Validation Plan

Gate C validation completed on 2026-05-05:

- focused Gate C passed 1 file / 6 tests;
- engine continuity passed 6 files / 37 tests;
- focused web Gate B compatibility passed 1 file / 7 tests after the
  selected Gate C file landed;
- `pnpm calculator:gate:current` passed with engine 250 files / 1448
  tests, web 53 files / 259 passed + 18 skipped, repo build 5 / 5
  tasks, and whitespace guard green;
- final `git diff --check` was green after restoring
  `apps/web/next-env.d.ts` to `.next-typecheck`.

Broad `pnpm check` was not run because Gate C made no runtime, API,
shared-schema, route/report, or workbench-input behavior change. Known
non-fatal `sharp/@img` warnings remain through `@turbodocx/html-to-docx`.

Focused Gate C command:

```sh
pnpm --filter @dynecho/engine exec vitest run src/post-rockwool-triple-leaf-explicit-screening-only-policy-v1-next-slice-selection-contract.test.ts --maxWorkers=1
```

Engine continuity should include:

```sh
pnpm --filter @dynecho/engine exec vitest run src/post-rockwool-triple-leaf-explicit-screening-only-policy-v1-next-slice-selection-contract.test.ts src/rockwool-triple-leaf-explicit-screening-only-policy-gate-a-contract.test.ts src/calculator-source-gap-revalidation-v23-gate-a-contract.test.ts src/company-internal-misclassification-readiness-blocker-contract.test.ts src/raw-wall-hostile-input-answer-matrix.test.ts src/raw-floor-hostile-input-answer-matrix.test.ts --maxWorkers=1
```

Then run:

```sh
pnpm calculator:gate:current
git diff --check
```

Gate B validation completed on 2026-05-05:

- focused Gate B passed 1 file / 7 tests;
- web visible continuity passed 5 files / 31 tests;
- engine continuity passed 5 files / 29 tests;
- `pnpm calculator:gate:current` passed with engine 249 files / 1442
  tests, web 53 files / 259 passed + 18 skipped, repo build 5 / 5
  tasks, and whitespace guard green;
- broad `pnpm check` passed with lint/typecheck clean, engine 382 files
  / 2262 tests, web 165 files / 932 passed + 18 skipped, and repo build
  5 / 5 tasks;
- final `git diff --check` was green after restoring
  `apps/web/next-env.d.ts` to `.next-typecheck`.

Known non-fatal `sharp/@img` warnings remain through
`@turbodocx/html-to-docx`.

Focused Gate B command:

```sh
pnpm --filter @dynecho/web exec vitest run features/workbench/rockwool-triple-leaf-explicit-screening-only-policy-gate-b-visible.test.ts --maxWorkers=1
```

Visible continuity:

```sh
pnpm --filter @dynecho/web exec vitest run features/workbench/rockwool-triple-leaf-explicit-screening-only-policy-gate-b-visible.test.ts features/workbench/company-internal-frequent-combination-lane-snapshot-guard-gate-b-visible.test.ts features/workbench/wall-triple-leaf-company-internal-acceptance-rehearsal.test.ts features/workbench/wall-flat-list-multileaf-family-guard-route-card.test.ts features/workbench/field-output-owner-and-design-grade-policy-gate-b-visible.test.ts --maxWorkers=1
```

Engine continuity:

```sh
pnpm --filter @dynecho/engine exec vitest run src/rockwool-triple-leaf-explicit-screening-only-policy-gate-a-contract.test.ts src/calculator-source-gap-revalidation-v23-gate-a-contract.test.ts src/post-field-output-owner-and-design-grade-policy-v1-next-slice-selection-contract.test.ts src/company-internal-misclassification-readiness-blocker-contract.test.ts src/wall-triple-leaf-uris2006-source-packet-acquisition-gate-u.test.ts --maxWorkers=1
```

Final gates:

```sh
pnpm calculator:gate:current
pnpm check
git diff --check
```

Gate A validation completed on 2026-05-05:

- focused Gate A passed 1 file / 6 tests;
- engine continuity passed 5 files / 29 tests;
- web visible continuity passed 3 files / 16 tests;
- `pnpm calculator:gate:current` passed with engine 249 files / 1442
  tests, web 52 files / 252 passed + 18 skipped, repo build 5 / 5
  tasks, and whitespace guard green;
- final `git diff --check` was green after restoring
  `apps/web/next-env.d.ts` to `.next-typecheck`.

Broad `pnpm check` was not run because Gate A made no runtime, visible,
API, shared-schema, route/report, or workbench-input behavior change.
Known non-fatal `sharp/@img` warnings remain through
`@turbodocx/html-to-docx`.

Prior Gate A focused command:

```sh
pnpm --filter @dynecho/engine exec vitest run src/rockwool-triple-leaf-explicit-screening-only-policy-gate-a-contract.test.ts --maxWorkers=1
```

Continuity should include:

```sh
pnpm --filter @dynecho/engine exec vitest run src/calculator-source-gap-revalidation-v23-gate-a-contract.test.ts src/post-field-output-owner-and-design-grade-policy-v1-next-slice-selection-contract.test.ts src/company-internal-misclassification-readiness-blocker-contract.test.ts src/wall-triple-leaf-uris2006-source-packet-acquisition-gate-u.test.ts --maxWorkers=1
```

Visible continuity should include:

```sh
pnpm --filter @dynecho/web exec vitest run features/workbench/wall-triple-leaf-company-internal-acceptance-rehearsal.test.ts features/workbench/wall-flat-list-multileaf-family-guard-route-card.test.ts features/workbench/field-output-owner-and-design-grade-policy-gate-b-visible.test.ts --maxWorkers=1
```

Then run:

```sh
pnpm calculator:gate:current
git diff --check
```

Run broad `pnpm check` if Gate A or a later visible gate moves wording,
route/report surfaces, API behavior, shared schemas, or runtime
calculator behavior.
