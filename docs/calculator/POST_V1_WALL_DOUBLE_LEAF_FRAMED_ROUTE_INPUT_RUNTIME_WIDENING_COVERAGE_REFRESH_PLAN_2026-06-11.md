# Post-V1 Wall Double-Leaf Framed Route-Input Runtime Widening Coverage Refresh Plan - 2026-06-11

Document role: selected no-runtime closeout after
`post_v1_wall_double_leaf_framed_route_input_runtime_widening_surface_parity_plan`.
This is a bounded coverage refresh for a landed calculator route-input
surface, not a broad source crawl, report polish, auth/storage work, or
another generic planning ledger.

Selected next action:
`post_v1_wall_double_leaf_framed_route_input_runtime_widening_coverage_refresh_plan`.

Selected next file:
`packages/engine/src/post-v1-wall-double-leaf-framed-route-input-runtime-widening-coverage-refresh-contract.test.ts`.

Selected next label:
`post-V1 wall double-leaf/framed route-input runtime widening coverage refresh`.

Previous surface parity:
`post_v1_wall_double_leaf_framed_route_input_runtime_widening_surface_parity_plan`.

Previous surface parity file:
`apps/web/features/workbench/post-v1-wall-double-leaf-framed-route-input-runtime-widening-surface-parity.test.ts`.

Previous surface parity status:
`post_v1_wall_double_leaf_framed_route_input_runtime_widening_surface_parity_landed_no_runtime_selected_coverage_refresh`.

Previous runtime owner:
`post_v1_wall_double_leaf_framed_route_input_runtime_widening_plan`.

Previous runtime owner file:
`packages/engine/src/post-v1-wall-double-leaf-framed-route-input-runtime-widening-contract.test.ts`.

Previous runtime owner status:
`post_v1_wall_double_leaf_framed_route_input_runtime_widening_landed_runtime_selected_surface_parity`.

The landed route-input widening opens a context-only empty-cavity
double-leaf/framed wall request where the visible stack is two board
leaves and the cavity is supplied as physical input:
`cavity1DepthMm 90`, `cavity1FillCoverage empty`,
`cavity1AbsorptionClass none`, side leaf groups, independent support,
and support spacing. It calculates element-lab `Rw 42`, `STC 42`,
`C -1`, and `Ctr -6.1` through runtime basis
`layer_combination_resolver_double_leaf_framed_wall_banded_source_absent_formula_corridor`.
The surface parity proves live workbench calculation, calculator API,
saved replay, server snapshot replay, output cards, target-output
status, and report summaries preserve that request shape and result.

Previous runtime counters: `newCalculableLayerTemplates: 1`,
`newCalculableRequestShapes: 1`, `newCalculableTargetOutputs: 4`,
`runtimeBasisPromotions: 1`, `frontendImplementationFilesTouched: 0`,
`runtimeValuesMoved 0`, `runtimeFormulaRetunes: 0`, and
`sourceRowsImported: 0`.

Previous surface counters: `webSurfaceParityContractFilesTouched: 1`,
`frontendImplementationFilesTouched: 0`, `newCalculableLayerTemplates: 0`,
`newCalculableRequestShapes: 0`, `newCalculableTargetOutputs: 0`,
`runtimeBasisPromotions: 0`, `runtimeValuesMoved 0`,
`runtimeFormulaRetunes: 0`, and `sourceRowsImported: 0`.

## Refresh Scope

The coverage refresh should freeze the landed route across the resolver
registry, runtime surface, coverage matrix, and company-internal V0
ledger as a source-absent formula route that is ready only when the
route-required physical inputs are present.

The refresh must re-probe:

- context-only empty-cavity two-board wall, element-lab `Rw 42`,
  `STC 42`, `C -1`, and `Ctr -6.1`;
- context-only cavity depth without explicit `empty`/`none`
  classification, still `needs_input` for `cavity1FillCoverage` and
  `absorberClass`;
- context-only absorptive cavity without visible porous layer, still
  `needs_input` for `cavity1FillCoverage` and `absorberClass`;
- direct-fixed context-only empty cavity, still on the direct-fixed
  Gate EO/ER owner;
- exact measured rows and compatible measured-anchor deltas, still
  higher precedence than the source-absent formula route;
- thick-board Auto, field/building aliases, ASTM/IIC/AIIC, grouped
  triple-leaf, multicavity, and floor-impact boundaries.

## Boundaries

Do not retune the double-leaf/framed formula, import source rows, widen
field/building adapters, or weaken the thick-board Auto family guard in
this closeout. If a coverage matrix or company-internal row is missing,
add only the bounded row needed for this already-landed request shape.

Expected counters for the follow-up:
`coverageRefreshContractFilesTouched: 1`,
`frontendImplementationFilesTouched: 0`, `newCalculableLayerTemplates: 0`,
`newCalculableRequestShapes: 0`, `newCalculableTargetOutputs: 0`,
`runtimeBasisPromotions: 0`, `runtimeValuesMoved 0`,
`runtimeFormulaRetunes: 0`, and `sourceRowsImported: 0`.

## Landed Closeout Update

Status:
`post_v1_wall_double_leaf_framed_route_input_runtime_widening_coverage_refresh_landed_no_runtime_selected_context_absorptive_cavity_input_owner`.

The closeout freezes the context-only empty-cavity route in the resolver
registry, runtime candidate adapter, runtime surface, coverage matrix,
and company-internal V0 posture. The route remains `ready_with_budget`
and `allowed_with_budget` on runtime basis
`layer_combination_resolver_double_leaf_framed_wall_banded_source_absent_formula_corridor`
with selected candidate
`candidate_layer_combination_resolver_double_leaf_framed_wall_banded_source_absent_family_solver`.
It re-probes `Rw 42`, `STC 42`, `C -1`, and `Ctr -6.1`, keeps
unclassified and context-only absorptive cavities on `needs_input`, and
preserves direct-fixed, exact/source, compatible anchor-delta,
thick-board Auto, field/building alias, ASTM/IIC/AIIC, grouped
triple-leaf, multicavity, and floor-impact boundaries. This is not a
broad source crawl.

Selected next action:
`post_v1_wall_double_leaf_framed_context_absorptive_cavity_input_owner_plan`.

Selected next file:
`packages/engine/src/post-v1-wall-double-leaf-framed-context-absorptive-cavity-input-owner-contract.test.ts`.

Selected next plan doc:
`docs/calculator/POST_V1_WALL_DOUBLE_LEAF_FRAMED_CONTEXT_ABSORPTIVE_CAVITY_INPUT_OWNER_PLAN_2026-06-11.md`.

Selected next label:
`post-V1 wall double-leaf/framed context absorptive cavity input owner`.

The selected next is value-moving. Its route family is
`wall.double_leaf_framed`; target outputs are `Rw`, `STC`, `C`, and
`Ctr`; required physical inputs include side leaf groups, cavity depth,
non-direct-fixed support topology, support spacing, `cavity1FillCoverage
full/partial`, `cavity1AbsorptionClass porous_absorptive`, and explicit
context-level absorber flow-resistivity ownership. Expected scope:
context-owned absorptive cavity assemblies calculate without forcing a
visible porous layer.
