# Post-V1 Gate EP/EQ Wall Direct-Fixed Double-Leaf Field/Building Adapter Plan - 2026-06-07

Document role: active post-Gate EO plan for the next calculator
scope/accuracy move. This is not a source-row crawl, confidence-label
pass, frontend task, or new formula invention. It is a formula-routing
and adapter-ownership plan for a complete visible wall subset.

## Gate EP Result

Gate EP lands as:

`post_v1_next_numeric_coverage_gap_gate_ep_plan`

Gate EP status:

`post_v1_next_numeric_coverage_gap_gate_ep_landed_no_runtime_selected_wall_direct_fixed_double_leaf_field_building_adapter_owner_gate_eq`

Selected candidate:

`wall.direct_fixed_double_leaf_field_building_adapter_owner_gap`

Selected next action:

`post_v1_wall_direct_fixed_double_leaf_field_building_adapter_owner_gate_eq_plan`

Selected next file:

`packages/engine/src/post-v1-wall-direct-fixed-double-leaf-field-building-adapter-owner-gate-eq-contract.test.ts`

Readable selected next label: Gate EQ, the direct-fixed double-leaf
field/building adapter owner proof.

Gate EP is no-runtime. Counters: `candidateCount 9`,
`roiAnalysisIterations: 2`, `estimatedNextAdapterOwnerLedgers 2`,
`estimatedNextNewCalculableLayerTemplates 1`,
`estimatedNextNewCalculableRequestShapes 3`,
`estimatedNextRuntimeCorrectedLayerTemplates 1`,
`estimatedNextRuntimeCorrectedRequestShapes 3`,
`newCalculableLayerTemplates 0`, `newCalculableRequestShapes 0`,
`runtimeBasisPromotions 0`, `runtimeFormulaRetunes 0`,
`runtimeValuesMoved 0`, `sourceRowsImported: 0`, and
`frontendImplementationFilesTouched: 0`.

## Current Runtime Evidence

Gate EO closed the element-lab direct-fixed double-leaf bridge-loss
route. The representative direct-fixed empty-cavity stack
`gypsum_board 12.5 / air_gap 45 / gypsum_board 12.5`, with explicit
`supportTopology=direct_fixed`, `connectionType=direct_fix`,
`studSpacingMm 400`, leaf groups, and cavity owner inputs, now selects:

`wall.direct_fixed_double_leaf.equivalent_coupled_mass_bridge_loss_owner`

It calculates `Rw 31 / STC 31 / C -1.2 / Ctr -5.9` with selected
candidate:

`candidate_wall_direct_fixed_double_leaf_bridge_loss_equivalent_coupled_mass`

The same complete direct-fixed stack still does not have a route-owned
field/building adapter:

- complete `field_between_rooms` requests for `R'w`, `Dn,w`, and
  `DnT,w` currently support the requested outputs but select
  `candidate_multileaf_screening_fallback` with method
  `screening_mass_law_curve_seed_v3`, calculating `R'w 25 / Dn,w 25 /
  DnT,w 28`;
- complete `building_prediction` requests for `R'w`, `Dn,w`, and
  `DnT,w` currently stop as unsupported with method
  `dynamic_calculator_building_prediction_runtime_adapter_owner_missing`;
- missing `receivingRoomRt60S` remains `needs_input` with exact missing
  field `receivingRoomRt60S`;
- missing `supportSpacingMm` remains a Gate EO `needs_input` boundary;
- independent and resilient double-leaf systems remain on the existing
  Gate S corridor and are not part of this selected gap.

## Gate EP Iteration 1

Subtract work that is already closed or correctly blocked:

- Gate EO already owns the element-lab direct-fixed `Rw`, STC, `C`, and
  `Ctr` route;
- Gate S plus the existing Gate I/AR adapters already cover
  non-direct-fixed double-leaf/framed field/building routes when their
  required physical inputs are present;
- missing `supportSpacingMm`, missing `receivingRoomRt60S`, and
  incomplete building-prediction context remain correct `needs_input`
  stops;
- broad source crawling, confidence wording, and frontend work do not
  choose the next formula route.

Result: redoing Gate EO lab runtime, redoing non-direct-fixed Gate S
field/building routes, or guessing missing field inputs is not the next
calculator slice.

## Gate EP Iteration 2

Rank the remaining candidates against the calculator advancement test:

1. `wall.direct_fixed_double_leaf_field_building_adapter_owner_gap`
   wins. It improves accuracy for complete field requests that currently
   use a weaker screening route and prepares scope expansion for
   complete building-prediction requests that are still unsupported.
2. Direct-fixed lab formula retuning is rejected until same-family
   holdout rows exist.
3. Lab-to-field/building aliasing is rejected. `Rw`/STC must not become
   `R'w`/`Dn,w`/`DnT,w` without explicit field/building adapters.
4. Source-row or residual tightening is useful later, but it does not
   beat the current route-selection defect.

Result: Gate EP selects Gate EQ as an owner-proof gate, not runtime
movement yet.

## Gate EQ Work Order

Gate EQ must prove the adapter owner before runtime values move.

Required owner proof:

- accept the Gate EO direct separating-element curve as a valid base
  curve for direct-fixed double-leaf field/building adapters;
- prove the field adapter owns `contextMode=field_between_rooms`,
  partition area from `panelWidthMm`/`panelHeightMm`,
  `receivingRoomVolumeM3`, and `receivingRoomRt60S`;
- prove the building adapter owns `contextMode=building_prediction`,
  partition area, source/receiving room volumes, RT60,
  `flankingJunctionClass`, `conservativeFlankingAssumption`,
  `buildingPredictionOutputBasis`, and `junctionCouplingLengthM`;
- keep lab `Rw`/STC/`C`/`Ctr` separate from `R'w`/`Dn,w`/`DnT,w`;
- keep missing field/building inputs as `needs_input`;
- keep multicavity/triple-leaf direct-fixed routes out of scope;
- move no runtime values in Gate EQ.

If Gate EQ lands, the next runtime gate should route this complete
direct-fixed subset through the Gate EO direct curve plus the already
owned field/building adapter semantics. Expected future movement:
`estimatedNextNewCalculableLayerTemplates 1`,
`estimatedNextNewCalculableRequestShapes 3`,
`estimatedNextRuntimeCorrectedLayerTemplates 1`, and
`estimatedNextRuntimeCorrectedRequestShapes 3`.

## Non-Goals

- Do not crawl random product rows to create a finite scenario library.
- Do not retune Gate EO without same-family holdouts.
- Do not make a frontend/workbench slice.
- Do not relabel lab ratings as field or building ratings.
- Do not weaken `needs_input` for missing support spacing, room RT60, or
  building/flanking context.
