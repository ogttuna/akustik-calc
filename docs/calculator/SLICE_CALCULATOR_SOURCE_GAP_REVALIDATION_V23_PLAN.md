# Slice Plan - Calculator Source Gap Revalidation V23

Slice id: `calculator_source_gap_revalidation_v23`

Status: GATE A LANDED / SELECTED ROCKWOOL EXPLICIT SCREENING-ONLY POLICY

Selected by:

`field_output_owner_and_design_grade_policy_v1` Gate C

Selection status:

`closed_field_output_owner_design_grade_policy_no_runtime_and_selected_source_gap_revalidation_v23`

Selected first file:

`packages/engine/src/calculator-source-gap-revalidation-v23-gate-a-contract.test.ts`

Selected first action:

`gate_a_revalidate_source_accuracy_gap_order_after_field_output_owner_policy_closeout`

Prior checkpoint:

`docs/calculator/CHECKPOINT_2026-05-05_FIELD_OUTPUT_OWNER_AND_DESIGN_GRADE_POLICY_GATE_C_CLOSEOUT_HANDOFF.md`

Planning surface:

`docs/calculator/SLICE_CALCULATOR_SOURCE_GAP_REVALIDATION_V23_PLAN.md`

Required carry-forward artifacts:

- `field_output_owner_policy_gate_c_closeout_summary`
- `rockwool_uris_exact_runtime_still_blocked`
- `rockwool_explicit_screening_only_policy_selected`
- `source_promotion_and_hostile_input_ownership_carry_forward`
- `repeat_uris_acquisition_blocked_without_new_packet`
- `pre_company_internal_use_exit_criteria`

## Landed Gate A

Landed status:

`selected_rockwool_triple_leaf_explicit_screening_only_policy_after_v23_confirmed_uris_source_blocked_and_field_output_owner_closed`

Landed file:

`packages/engine/src/calculator-source-gap-revalidation-v23-gate-a-contract.test.ts`

Selected next slice:

`rockwool_triple_leaf_explicit_screening_only_policy_v1`

Selected next action:

`gate_a_inventory_rockwool_triple_leaf_screening_only_policy_after_v23_rerank`

Selected next file:

`packages/engine/src/rockwool-triple-leaf-explicit-screening-only-policy-gate-a-contract.test.ts`

Selected planning surface:

`docs/calculator/SLICE_ROCKWOOL_TRIPLE_LEAF_EXPLICIT_SCREENING_ONLY_POLICY_V1_PLAN.md`

Gate A produced:

- `field_output_owner_policy_gate_c_closeout_summary`
- `rockwool_uris_exact_runtime_still_blocked`
- `rockwool_explicit_screening_only_policy_selected`
- `source_promotion_and_hostile_input_ownership_carry_forward`
- `repeat_uris_acquisition_blocked_without_new_packet`
- `pre_company_internal_use_exit_criteria`

Gate A is no-runtime. Runtime values, support, confidence, evidence,
API behavior, route-card values, output-card status, proposal/report
values, and workbench-input behavior stayed frozen.

## Objective

Re-rank the current source/accuracy backlog after field-output owner and
design-grade policy closed no-runtime. V23 must decide the next bounded
calculator correctness step without implying that finite field outputs
are design-grade and without hiding the Rockwool triple-leaf defect.

V23 is not pre-approved runtime promotion. It may select a runtime
slice only if the selected row names exact topology, local material
mapping, metric context, tolerance owner, protected negative
boundaries, source/curve provenance, and paired engine/web/report
tests. If those owners are still missing, V23 must select a bounded
no-runtime accuracy, source, or guardrail step.

## Inputs From Field-Output Owner Gate C

`field_output_owner_policy_gate_c_closeout_summary`

Gate A inventoried field-output owner requirements and Gate B made the
visible output-card / proposal-report policy explicit. Gate C closed
that slice without runtime movement.

`field_output_owner_and_design_grade_policy_closed_carry_forward`

Finite `R'w`, `DnT,w`, `DnT,A`, `L'n,w`, `L'nT,w`, and `L'nT,50`
remain non-design-grade. A future design-grade field output still needs
metric owner, source basis owner, geometry/room context owner,
tolerance owner, negative boundaries, and paired engine/web/report
tests.

`rockwool_rw41_screening_and_uris_packet_status`

Rockwool triple-leaf remains not fixed. Grouped split-rockwool is still
`Rw 41`, `multileaf_screening_blend`, low confidence, screening-only,
not exact, and not source-validated.

`repeat_uris_acquisition_blocked_without_new_packet`

Uris 2006 remains `paused_waiting_rights_safe_source_packet`. Do not
repeat acquisition unless a new rights-safe source-owned curve payload,
authorized TDM payload, page-image packet, numeric band-vector packet,
or equivalent source packet exists.

`pre_company_internal_use_exit_criteria`

The field-output owner criterion is now closed, but company-internal
high-accuracy opening remains blocked until Rockwool is exact or
explicitly screening-only, source promotion ownership closes, hostile
API/import payloads fail closed, and full validation is green at the
opening handoff.

## Candidate Order For Gate A

Gate A must compare at least:

1. Rockwool/Uris exact source-owned runtime readiness.
2. Explicit Rockwool screening-only / company-opening blocker closure
   if exact runtime remains blocked.
3. Source-promotion ownership for near-source aliases and source rows.
4. Hostile API/import and frequent-combination guardrail gaps.
5. Field-output owner policy follow-up only if Gate C or visible tests
   found a remaining path.
6. Productization/report polish only after correctness blockers are
   controlled.

No candidate may use visible copy, source locator metadata, a finite
screening number, or a near-source alias as runtime evidence.

## Detailed Gate A Plan

1. Create
   `packages/engine/src/calculator-source-gap-revalidation-v23-gate-a-contract.test.ts`.
2. Freeze runtime values, output support, confidence, evidence, API,
   route-card values, output-card status, proposal/report values, and
   workbench-input behavior unless a later selected slice explicitly
   names a complete runtime owner set.
3. Recompute the grouped split-rockwool and flat-list swap snapshots.
   Grouped split-rockwool must remain `Rw 41`,
   `multileaf_screening_blend`, low confidence, screening-only, not
   exact, and not source-validated. Flat-list swaps must remain
   fail-closed until grouped topology is supplied.
4. Carry forward Uris 2006 as
   `paused_waiting_rights_safe_source_packet` unless a new rights-safe
   packet exists in the repo or is explicitly supplied.
5. Assert field-output owner policy is closed as visible policy, but
   does not make field outputs design-grade or exact field
   measurements.
6. Re-rank candidate rows and select exactly one next bounded file with
   target validation scope.

## Validation Plan

Gate A validation completed on 2026-05-05:

- focused V23 passed 1 file / 7 tests;
- engine continuity passed 6 files / 36 tests;
- web visible continuity passed 3 files / 16 tests;
- `pnpm calculator:gate:current` passed with engine 248 files / 1436
  tests, web 52 files / 252 passed + 18 skipped, repo build 5 / 5
  tasks, and whitespace guard green;
- final `git diff --check` was green after restoring
  `apps/web/next-env.d.ts` to `.next-typecheck`.

Broad `pnpm check` was not run because V23 made no runtime, visible,
API, shared-schema, route/report, or workbench-input behavior change.
Known non-fatal `sharp/@img` warnings remain through
`@turbodocx/html-to-docx`.

After V23 Gate A is implemented:

```sh
pnpm --filter @dynecho/engine exec vitest run src/calculator-source-gap-revalidation-v23-gate-a-contract.test.ts --maxWorkers=1
```

Continuity should include at least:

```sh
pnpm --filter @dynecho/engine exec vitest run src/post-field-output-owner-and-design-grade-policy-v1-next-slice-selection-contract.test.ts src/field-output-owner-and-design-grade-policy-gate-a-contract.test.ts src/calculator-source-gap-revalidation-v22-gate-a-contract.test.ts src/company-internal-misclassification-readiness-blocker-contract.test.ts --maxWorkers=1
```

Then run:

```sh
pnpm calculator:gate:current
git diff --check
```

Use broad `pnpm check` if V23 moves visible behavior, runtime behavior,
shared schemas, API contracts, or docs enough to require the full gate.
