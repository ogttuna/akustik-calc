# Post-V1 Wall Compatible Anchor-Delta STC-Only Lab Companion Coverage Refresh Plan - 2026-06-11

Document role: selected no-runtime closeout after the STC-only lab
companion surface parity. This is not a broad source crawl, UI polish,
or formula retune. It exists only to freeze the newly visible calculator
scope and then force the next follow-up back to a value-moving formula,
adapter, input-surface, anchor, calibration, or holdout slice.

Plan doc path:
`docs/calculator/POST_V1_WALL_COMPATIBLE_ANCHOR_DELTA_STC_ONLY_LAB_COMPANION_COVERAGE_REFRESH_PLAN_2026-06-11.md`.

## Dependency

Runtime owner:
`post_v1_wall_compatible_anchor_delta_stc_only_lab_companion_owner_plan`.

Runtime owner file:
`packages/engine/src/post-v1-wall-compatible-anchor-delta-stc-only-lab-companion-owner-contract.test.ts`.

Runtime owner status:
`post_v1_wall_compatible_anchor_delta_stc_only_lab_companion_owner_landed_runtime_selected_surface_parity`.

Surface parity:
`post_v1_wall_compatible_anchor_delta_stc_only_lab_companion_surface_parity_plan`.

Surface parity file:
`apps/web/features/workbench/post-v1-wall-compatible-anchor-delta-stc-only-lab-companion-surface-parity.test.ts`.

Surface parity status:
`post_v1_wall_compatible_anchor_delta_stc_only_lab_companion_surface_parity_landed_no_runtime_selected_coverage_refresh`.

The owner and surface together expose paired exterior-board `STC 59` and
one-side exterior-board `STC 57` for Knauf `416889` compatible
anchor-delta element-lab STC-only requests through
`wall.compatible_anchor_delta.calculated_lab_companions` with runtime
basis
`post_v1_wall_compatible_anchor_delta_calculated_lab_companion_runtime`.

## Selected Next

Selected next action:
`post_v1_wall_compatible_anchor_delta_stc_only_lab_companion_coverage_refresh_plan`.

Selected next file:
`packages/engine/src/post-v1-wall-compatible-anchor-delta-stc-only-lab-companion-coverage-refresh-contract.test.ts`.

Selected next label:
`post-V1 wall compatible anchor-delta STC-only lab companion coverage refresh`.

Closeout status:
`post_v1_wall_compatible_anchor_delta_stc_only_lab_companion_coverage_refresh_landed_no_runtime_selected_c_ctr_only_lab_companion_owner`.

## Iteration 1 - Closeout Scope

The coverage refresh should re-probe only the just-landed STC-only lane:

- paired exterior-board Knauf `416889` element-lab `STC 59`;
- one-side exterior-board Knauf `416889` element-lab `STC 57`;
- selected candidate:
  `wall.compatible_anchor_delta.calculated_lab_companions`;
- runtime basis:
  `post_v1_wall_compatible_anchor_delta_calculated_lab_companion_runtime`;
- workbench/API/replay/report visibility from the surface parity file.

This refresh must not import source rows, retune the shifted direct curve,
or claim measured STC evidence.

## Iteration 2 - Boundaries To Keep Frozen

- Direct single `Rw` stays on
  `wall.compatible_anchor_delta.extra_board_on_verified_lsf`.
- Mixed `Rw+STC/C/Ctr` stays on the existing calculated lab companion
  route.
- C-only and Ctr-only remain unsupported until a separate owner selects
  them.
- Field/building, A-weighted, ASTM/IIC/AIIC, missing-input, and
  non-Knauf requests remain outside this STC-only owner.

## Iteration 3 - Follow-Up Rule

After this closeout lands, do not select another standalone
process-only ledger by default. The next selected slice must declare:

- route family;
- target output or accuracy term;
- current engine state: calculable, `needs_input`, or `unsupported`;
- required physical inputs or evidence;
- expected scope/accuracy movement;
- pinned metric-basis boundaries.

Expected closeout counters:
`coverageRefreshContractFilesTouched: 1`,
`webSurfaceParityContractFilesTouched: 1`,
`frontendImplementationFilesTouched: 0`,
`newCalculableRequestShapes: 0`, `runtimeBasisPromotions: 0`,
`runtimeValuesMoved 0`, `runtimeFormulaRetunes: 0`,
`sourceRowsImported: 0`, `estimatedNextCalculableRequestShapes: 4`,
and `estimatedNextRuntimeBasisPromotions: 1`.

## Landed Closeout

The coverage refresh freezes paired exterior-board `STC 59` and
one-side exterior-board `STC 57` for Knauf `416889` compatible
anchor-delta element-lab STC-only requests through
`wall.compatible_anchor_delta.calculated_lab_companions` with runtime
basis
`post_v1_wall_compatible_anchor_delta_calculated_lab_companion_runtime`.
The resolver registry, runtime adapter, runtime surface, coverage matrix,
and company-internal V0 keep the candidate `ready_with_budget` /
`allowed_with_budget` with visible not-measured STC/C/Ctr budget terms.
Direct `Rw`, mixed `Rw+STC/C/Ctr`, C/Ctr-only, field/building,
A-weighted, ASTM/IIC/AIIC, missing-input, and non-Knauf boundaries remain
pinned. This is not a broad source crawl.

## Selected Value-Moving Follow-Up

Selected next action:
`post_v1_wall_compatible_anchor_delta_c_ctr_only_lab_companion_owner_plan`.

Selected next file:
`packages/engine/src/post-v1-wall-compatible-anchor-delta-c-ctr-only-lab-companion-owner-contract.test.ts`.

Selected next plan doc:
`docs/calculator/POST_V1_WALL_COMPATIBLE_ANCHOR_DELTA_C_CTR_ONLY_LAB_COMPANION_OWNER_PLAN_2026-06-11.md`.

Selected next label:
`post-V1 wall compatible anchor-delta C/Ctr-only lab companion owner`.

The selected next is value-moving: standalone C-only and Ctr-only
element-lab requests are currently unsupported even though the mixed
lab companion route already owns paired `C -1.1 / Ctr -6` and one-side
`C -0.6 / Ctr -5.5` from the same shifted curve. The follow-up should not
import source rows, retune formulas, or broaden field/building,
A-weighted, ASTM/IIC/AIIC, non-selected-anchor, or missing-input
boundaries.
