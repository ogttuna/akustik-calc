# Post-V1 Wall Double-Leaf Framed Context Absorptive Cavity Input Owner Surface Parity Plan - 2026-06-11

Document role: selected no-runtime surface parity follow-up after the
value-moving context absorptive cavity input owner. This is calculator
surface work: it must keep workbench calculation, API payloads, saved
replay, server replay, output cards, target-output status, and report
summaries aligned with the engine-owned runtime values. It is not UI
polish, report polish, a source-row crawl, auth/storage work, or another
numeric rerank.

Selected next action:
`post_v1_wall_double_leaf_framed_context_absorptive_cavity_input_owner_surface_parity_plan`.

Selected next file:
`apps/web/features/workbench/post-v1-wall-double-leaf-framed-context-absorptive-cavity-input-owner-surface-parity.test.ts`.

Selected next plan doc:
`docs/calculator/POST_V1_WALL_DOUBLE_LEAF_FRAMED_CONTEXT_ABSORPTIVE_CAVITY_INPUT_OWNER_SURFACE_PARITY_PLAN_2026-06-11.md`.

Selected next label:
`post-V1 wall double-leaf/framed context absorptive cavity input owner surface parity`.

Landed surface parity status:
`post_v1_wall_double_leaf_framed_context_absorptive_cavity_input_owner_surface_parity_landed_no_runtime_selected_coverage_refresh`.

Selected coverage refresh action:
`post_v1_wall_double_leaf_framed_context_absorptive_cavity_input_owner_coverage_refresh_plan`.

Selected coverage refresh file:
`packages/engine/src/post-v1-wall-double-leaf-framed-context-absorptive-cavity-input-owner-coverage-refresh-contract.test.ts`.

Selected coverage refresh plan doc:
`docs/calculator/POST_V1_WALL_DOUBLE_LEAF_FRAMED_CONTEXT_ABSORPTIVE_CAVITY_INPUT_OWNER_COVERAGE_REFRESH_PLAN_2026-06-11.md`.

Selected coverage refresh label:
`post-V1 wall double-leaf/framed context absorptive cavity input owner coverage refresh`.

Previous owner:
`post_v1_wall_double_leaf_framed_context_absorptive_cavity_input_owner_plan`.

Previous owner file:
`packages/engine/src/post-v1-wall-double-leaf-framed-context-absorptive-cavity-input-owner-contract.test.ts`.

Previous owner status:
`post_v1_wall_double_leaf_framed_context_absorptive_cavity_input_owner_landed_runtime_selected_surface_parity`.

Previous coverage refresh:
`post_v1_wall_double_leaf_framed_route_input_runtime_widening_coverage_refresh_plan`.

Previous coverage refresh file:
`packages/engine/src/post-v1-wall-double-leaf-framed-route-input-runtime-widening-coverage-refresh-contract.test.ts`.

Previous coverage refresh status:
`post_v1_wall_double_leaf_framed_route_input_runtime_widening_coverage_refresh_landed_no_runtime_selected_context_absorptive_cavity_input_owner`.

## Surface Contract

Route family: `wall.double_leaf_framed`.

Target outputs: element-lab `Rw`, `STC`, `C`, and `Ctr`.

Runtime basis:
`layer_combination_resolver_double_leaf_framed_wall_banded_source_absent_formula_corridor`.

Selected candidate:
`candidate_layer_combination_resolver_double_leaf_framed_wall_banded_source_absent_family_solver`.

Runtime owner values to surface:

- context-owned full absorptive cavity: `Rw 46`, `STC 46`, `C -1`,
  and `Ctr -6.1`;
- context-only empty cavity remains `Rw 42`, `STC 42`, `C -1`, and
  `Ctr -6.1`.

The engine owner maps explicit context-level absorber flow resistivity to
`flowResistivitySource=user_supplied`, applies the owned porous damping
credit, and keeps the result source-absent with visible budget terms. This
is not a measured-source claim and not a broad source crawl.

Owner counters: `newCalculableLayerTemplates: 1`,
`newCalculableRequestShapes: 1`, `newCalculableTargetOutputs: 4`,
`runtimeBasisPromotions: 1`, `runtimeValuesMoved 0`,
`runtimeFormulaRetunes: 0`, `sourceRowsImported: 0`, and
`frontendImplementationFilesTouched: 0`.

Landed surface counters: `webSurfaceParityContractFilesTouched: 1`,
`frontendImplementationFilesTouched: 1`, `runtimeValuesMoved 0`,
`runtimeFormulaRetunes: 0`, and `sourceRowsImported: 0`.

Implementation note: the surface closeout found that the existing
workbench `airborneAdvancedWallInputSurface` normalized cavity-only input
into an incomplete full Gate AY advanced-wall payload. A narrow adapter
now forwards cavity-only input as context-owned absorber data when there
are no panels, frame/coupling inputs, openings, output-basis,
frequency-band, or Gate AY solver intent. Complete Gate AY input surfaces
remain outside this owner.

## Boundaries To Preserve

The surface parity test must keep missing absorber ownership as
`needs_input`, not a hidden default. It must also keep complete Gate AY
advanced-wall inputs on the Gate AY owner, direct-fixed walls on the
direct-fixed double-leaf owner, exact/anchor rows ahead of the
source-absent formula, thick-board Auto on its needs-input boundary, and
field/building aliases plus ASTM/IIC/AIIC and impact outputs outside this
owner.

Expected counters for the surface follow-up:
`webSurfaceParityContractFilesTouched: 1`,
`frontendImplementationFilesTouched: 1`, `newCalculableLayerTemplates: 0`,
`newCalculableRequestShapes: 0`, `newCalculableTargetOutputs: 0`,
`runtimeBasisPromotions: 0`, `runtimeValuesMoved 0`,
`runtimeFormulaRetunes: 0`, and `sourceRowsImported: 0`.
