# Post-V1 Runtime-First Rerank After Current-Gate Stale Metric/Basis Reconciliation Plan - 2026-06-23

Status:
`post_v1_runtime_first_route_family_rerank_after_current_gate_stale_metric_basis_reconciliation_landed_no_runtime_selected_british_gypsum_exact_lab_field_building_adapter_owner`

## Purpose

This is the selected next action after the no-runtime current-gate
stale metric/basis reconciliation that followed the wall opening/leak
building apparent `Dn,w` / `Dn,A` companion chain.

The reconciliation landed because the current gate still carried stale
expectations after the opening/leak apparent companion owner: impact
routes expected airborne `Rw` / `C` / `Ctr` support, opening/leak
assertions expected building `Dn,A` to remain closed, resolver
precedence predated `user_verified_calculated_exact`, mixed
field/building probes leaked stale basis values, and flat multicavity
missing-input boundaries had drifted from current behavior.

This rerank must choose the next concrete calculator behavior. It is
not a broad source crawl, UI polish pass, confidence-labeling pass,
generic docs cleanup, or a second support-only loop. The next selected
slice must either move a runtime/accuracy/input-surface behavior, select
a targeted source/holdout prerequisite with a direct formula-route
payoff, or explicitly stop because no safe behavior can move.

Previous selected reconciliation:
`post_v1_current_gate_stale_metric_basis_reconciliation_after_opening_leak_apparent_dna_plan`
/
`packages/engine/src/post-v1-current-gate-stale-metric-basis-reconciliation-after-opening-leak-apparent-dna-contract.test.ts`
/
`docs/calculator/POST_V1_CURRENT_GATE_STALE_METRIC_BASIS_RECONCILIATION_AFTER_OPENING_LEAK_APPARENT_DNA_PLAN_2026-06-23.md`
/
`post_v1_current_gate_stale_metric_basis_reconciliation_after_opening_leak_apparent_dna_landed_no_runtime_selected_runtime_first_rerank_after_current_gate_reconciliation`.

Selected next action:
`post_v1_runtime_first_route_family_rerank_after_current_gate_stale_metric_basis_reconciliation_plan`

Selected next file:
`packages/engine/src/post-v1-runtime-first-route-family-rerank-after-current-gate-stale-metric-basis-reconciliation-contract.test.ts`

Selected next plan:
`docs/calculator/POST_V1_RUNTIME_FIRST_RERANK_AFTER_CURRENT_GATE_STALE_METRIC_BASIS_RECONCILIATION_PLAN_2026-06-23.md`

Selected next label:
`post-V1 runtime-first route-family rerank after current-gate stale metric/basis reconciliation`

Landed rerank status:
`post_v1_runtime_first_route_family_rerank_after_current_gate_stale_metric_basis_reconciliation_landed_no_runtime_selected_british_gypsum_exact_lab_field_building_adapter_owner`

Selected candidate:
`wall.british_gypsum_exact_lab_field_building_adapter_owner`

Selected follow-up action:
`post_v1_wall_british_gypsum_exact_lab_field_building_adapter_owner_plan`

Selected follow-up file:
`packages/engine/src/post-v1-wall-british-gypsum-exact-lab-field-building-adapter-owner-contract.test.ts`

Selected follow-up plan:
`docs/calculator/POST_V1_WALL_BRITISH_GYPSUM_EXACT_LAB_FIELD_BUILDING_ADAPTER_OWNER_PLAN_2026-06-23.md`

Selected follow-up label:
`post-V1 wall British Gypsum exact lab field/building adapter owner`

Reconciliation counters:

- `currentGateKnownFailureAssertionsReconciled: 34`;
- `staleExpectationFilesTouched: 22`;
- `newCalculableRequestShapes: 0`;
- `newCalculableTargetOutputs: 0`;
- `runtimeBasisPromotions: 0`;
- `runtimeValuesMoved 0`;
- `runtimeFormulaRetunes: 0`;
- `sourceRowsImported: 0`;
- `frontendImplementationFilesTouched: 1`;
- `webCurrentGateStaleFailureAssertionsReconciled: 4`;
- `webStaleExpectationFilesTouched: 4`.

## Selection Card

- User construction / formula family: runtime-first route-family rerank
  across the post-reconciliation calculator surface. The rerank must
  pick one bounded wall, floor, ceiling, or opening family before any
  runtime value moves.
- Target outputs to compare: `Rw`, `STC`, `C`, `Ctr`, `R'w`,
  `Dn,w`, `Dn,A`, `DnT,w`, `DnT,A`, `DnT,A,k`, `Ln,w`, `DeltaLw`,
  `IIC`, `AIIC`, and companion metrics where the route is physically
  owned.
- Route: no runtime calculation inside this rerank. The selected
  follow-up must name an exact measured row, same-family bounded anchor
  or delta, owned formula/adapter route, or precise `needs_input` /
  `unsupported` boundary.
- Required physical inputs: candidate-specific area, receiving-room
  volume, reverberation/normalization basis, flanking/junction context,
  opening/leak context, mounting/framing/topology, dynamic stiffness,
  load basis, flow resistivity, cavity depth, stud/joist spacing, and
  frequency backbone must be named before selection.
- `needs_input` behavior: missing route-required physical inputs must
  remain explicit and human-readable; the rerank must not default them
  silently.
- `unsupported` boundaries: lab/field/building basis aliases,
  impact/airborne aliases, ASTM/ISO aliases, source-proximate rows
  without same-basis proof, and broad family substitutions remain
  blocked.
- Expected `newCalculableRequestShapes`: 0 in the rerank itself.
- Expected `newCalculableTargetOutputs`: 0 in the rerank itself.
- Expected `runtimeBasisPromotions`: 0 in the rerank itself.
- Expected `runtimeValuesMoved`: 0 in the rerank itself.

Rerank counters: `candidateCount: 8`, `roiAnalysisIterations: 4`,
`estimatedNextCalculableRequestShapes: 4`,
`estimatedNextCalculableTargetOutputs: 12`,
`estimatedNextRequiredPhysicalInputsCaptured: 10`,
`estimatedNextRuntimeBasisPromotions: 4`,
`estimatedNextRuntimeValuesMoved: 12`, `runtimeValuesMoved 0`,
`runtimeFormulaRetunes: 0`, `sourceRowsImported: 0`, and
`frontendImplementationFilesTouched: 0`.

## ROI Iterations Before Implementation

Iteration 1: subtract completed support work.

- The opening/leak building `DnT,A,k`, apparent `Dn,w`, and apparent
  `Dn,A` chain is already landed and protected.
- The current-gate stale metric/basis reconciliation has removed known
  stale assertions from the gate.
- Another broad reconciliation or coverage-only pass would be a support
  loop unless it is a direct prerequisite for a named calculator
  behavior.

Iteration 2: prefer runtime/accuracy/input-surface payoff.

- High-ROI candidates should open a new calculable request shape,
  promote a correctly based target output, calibrate a formula family,
  or capture missing route-required inputs.
- Source work is acceptable only when it is a targeted same-family
  exact row, bounded anchor, or holdout prerequisite for a selected
  formula family.
- UI/report work is acceptable only if it is the selected input-surface
  owner for required physical inputs that block calculation.

Iteration 3: keep metric/basis discipline after the gate refresh.

- Impact-only routes must not re-advertise airborne outputs just to
  widen visible coverage.
- Lab values must not be copied into field/building outputs without an
  owned adapter.
- Nearby source rows must not become generic aliases.
- The selected next slice must be narrow enough to validate with
  targeted engine tests before any broad gate run.

## Work Order

1. Create the selected rerank contract at
   `packages/engine/src/post-v1-runtime-first-route-family-rerank-after-current-gate-stale-metric-basis-reconciliation-contract.test.ts`.
2. Rank at least one concrete runtime/accuracy candidate, one
   input-surface or required-input candidate, one source/holdout
   prerequisite candidate, and one blocked non-goal/source-crawl lane.
3. Select exactly one follow-up and record its construction family,
   target outputs, route, required inputs, missing-input behavior,
   unsupported boundaries, expected counters, implementation file, and
   plan doc.
4. Do not move runtime values in the rerank contract.
5. Keep `tools/dev/run-calculator-current-gate.ts`, `AGENTS.md`,
   `DOCUMENTATION_MAP.md`, `CURRENT_STATE.md`, `NEXT_AGENT_BRIEF.md`,
   `NEXT_IMPLEMENTATION_PLAN.md`, and `CALCULATOR_SOURCE_OF_TRUTH.md`
   synchronized.

## Validation Plan

- Run the current-gate stale metric/basis reconciliation contract.
- Run the selected rerank contract when it exists.
- Run the adjacent opening/leak apparent owner, coverage refresh,
  previous rerank, and reconciliation chain if the selected candidate
  still depends on those boundaries.
- Run `git diff --check`.
- Expand to `pnpm calculator:gate:current` after the selected next
  behavior is implemented or when the rerank changes the current-gate
  runner.

## Stop Rule

If the rerank cannot identify a concrete runtime/accuracy/input-surface
candidate with clear metric/basis boundaries, stop and document why no
safe calculator behavior can move. Do not keep chaining support-only
work.
