# Post-V1 Runtime-First Route-Family Rerank After Project/User Measured Wall Airborne Frequency Exact Curve Bridge Coverage Refresh - 2026-06-18

## Purpose

This is the selected next no-runtime rerank after
`post_v1_project_user_measured_wall_airborne_frequency_exact_curve_bridge_coverage_refresh_plan`.

Rerank action:
`post_v1_runtime_first_route_family_rerank_after_project_user_measured_wall_airborne_frequency_exact_curve_bridge_coverage_refresh_plan`

Rerank file:
`packages/engine/src/post-v1-runtime-first-route-family-rerank-after-project-user-measured-wall-airborne-frequency-exact-curve-bridge-coverage-refresh-contract.test.ts`

Rerank plan:
`docs/calculator/POST_V1_RUNTIME_FIRST_ROUTE_FAMILY_RERANK_AFTER_PROJECT_USER_MEASURED_WALL_AIRBORNE_FREQUENCY_EXACT_CURVE_BRIDGE_COVERAGE_REFRESH_PLAN_2026-06-18.md`

Rerank status:
`post_v1_runtime_first_route_family_rerank_after_project_user_measured_wall_airborne_frequency_exact_curve_bridge_coverage_refresh_landed_no_runtime_selected_project_user_measured_wall_airborne_frequency_compatible_delta_owner`

## Landing Update

Landed file:

`packages/engine/src/post-v1-runtime-first-route-family-rerank-after-project-user-measured-wall-airborne-frequency-exact-curve-bridge-coverage-refresh-contract.test.ts`

Selected candidate:

`project_user_measured_wall_airborne_frequency_compatible_delta_owner`

The rerank selected the project/user measured wall airborne frequency
compatible-delta owner. The exact measured curve bridge is already live
for full-stack element-lab matches, and the existing compatible-anchor
delta route proves a bounded exterior-board mass delta shape. The next
runtime owner should apply that bounded delta only when the reduced
current stack exactly matches an active measured curve anchor, then rate
the shifted measured curve for lab `Rw`, `STC`, `C`, and `Ctr`.

The rerank rejected broader user-material input widening, building/
flanking runtime, companion completeness, calibration/holdout work,
opening/leak/common-wall residuals, and UI/report/source cleanup because
they either need missing context/evidence, collide with other-agent
dirty work, risk metric aliasing, or are less bounded than the measured
curve delta owner.

Counters: `candidateCount: 8`, `roiAnalysisIterations: 4`,
`estimatedNextCalculableRequestShapes: 4`,
`estimatedNextCalculableTargetOutputs: 4`,
`estimatedNextRequiredPhysicalInputsCaptured: 6`,
`estimatedNextRuntimeBasisPromotions: 1`,
`estimatedNextRuntimeValuesMoved: 4`,
`estimatedNextUnsupportedBoundariesProtected: 7`,
`runtimeValuesMoved 0`, `runtimeFormulaRetunes: 0`,
`sourceRowsImported: 0`, and `frontendImplementationFilesTouched: 0`.

Selected next action:

`post_v1_project_user_measured_wall_airborne_frequency_compatible_delta_owner_plan`

Selected next file:

`packages/engine/src/post-v1-project-user-measured-wall-airborne-frequency-compatible-delta-owner-contract.test.ts`

Selected next plan:

`docs/calculator/POST_V1_PROJECT_USER_MEASURED_WALL_AIRBORNE_FREQUENCY_COMPATIBLE_DELTA_OWNER_PLAN_2026-06-18.md`

Selected next label:

`post-V1 project/user measured wall airborne frequency compatible-delta owner`

The rerank must choose the next calculator slice without drifting into
UI polish, report assistant work, broad source crawling, confidence
copy, or generic cleanup. It should compare route families by the
calculator north star:

1. exact measured/source rows only when construction and metric basis
   truly match;
2. same-family, same-basis compatible anchors or bounded deltas only
   when the boundary is proven;
3. otherwise owned physics/formula calculation with route-required
   physical inputs;
4. otherwise precise `needs_input` or `unsupported`.

## Previous Coverage Refresh

Coverage refresh action:
`post_v1_project_user_measured_wall_airborne_frequency_exact_curve_bridge_coverage_refresh_plan`

Coverage refresh file:
`packages/engine/src/post-v1-project-user-measured-wall-airborne-frequency-exact-curve-bridge-coverage-refresh-contract.test.ts`

Coverage refresh plan:
`docs/calculator/POST_V1_PROJECT_USER_MEASURED_WALL_AIRBORNE_FREQUENCY_EXACT_CURVE_BRIDGE_COVERAGE_REFRESH_PLAN_2026-06-18.md`

Coverage refresh status:
`post_v1_project_user_measured_wall_airborne_frequency_exact_curve_bridge_coverage_refresh_landed_no_runtime_selected_runtime_first_route_family_rerank`

Previous owner runtime method:
`post_v1_project_user_measured_wall_airborne_frequency_exact_curve_bridge`

Protected behavior:

- active `airborneMeasuredFrequencySourceAnchors` request intake;
- exact element-lab wall fingerprint matching only;
- declared-basis measured-curve derivation for lab `Rw`, `STC`, `C`,
  and `Ctr`;
- scalar `Rw` anchor separation;
- missing rating-standard and ambiguous exact-row boundaries;
- field/building/impact and mixed lab+field requests staying outside.

Previous owner action:
`post_v1_project_user_measured_wall_airborne_frequency_exact_curve_bridge_owner_plan`

Previous owner file:
`packages/engine/src/post-v1-project-user-measured-wall-airborne-frequency-exact-curve-bridge-owner-contract.test.ts`

Previous owner status:
`post_v1_project_user_measured_wall_airborne_frequency_exact_curve_bridge_owner_landed_runtime_selected_coverage_refresh`

Coverage counters: `coverageRefreshContractFilesTouched: 1`,
`newCalculableLayerTemplates: 0`, `newCalculableRequestShapes: 0`,
`newCalculableTargetOutputs: 0`, `runtimeBasisPromotions: 0`,
`runtimeValuesMoved 0`, `runtimeFormulaRetunes: 0`,
`sourceRowsImported: 0`, and `frontendImplementationFilesTouched: 0`.

## Candidate Set

The rerank should run at least four analysis iterations over these
families and may add a candidate only if it is calculator-owned and
bounded:

1. `user_material_physical_input_coverage_owner`
   - High strategic ROI.
   - Ready only if one route family names required physical inputs:
     topology, leaf grouping, support topology, support spacing, cavity
     depth, absorber flow resistivity, dynamic stiffness, load basis, or
     base structure.
   - Reject if it infers missing topology or uses material names as
     shortcuts.

2. `building_prediction_flanking_runtime_owner`
   - Very high market ROI for `R'w`, `Dn,w`, `DnT,w`, `L'n,w`, and
     `L'nT,w`.
   - Ready only when room volume, separating area, RT, junction/flanking
     context, and lab/field/building basis separation are explicit.
   - Reject if it copies lab ratings into field/building outputs.

3. `frequency_band_backbone_owned_route_owner`
   - Highest long-term accuracy ceiling.
   - Ready only when one concrete wall/floor/impact route can return an
     owned one-third-octave curve and derive companions from rating
     adapters.
   - Reject broad architecture rewrites or synthetic curves used only to
     unlock labels.

4. `companion_metric_completeness_from_owned_spectrum_owner`
   - High practical ROI where a route already carries spectrum/rating
     basis.
   - Ready when `Rw`, `STC`, `C`, `Ctr`, `OITC`, `Ln,w`, or IIC
     companions can be derived from an owned curve.
   - Reject scalar-only companion aliasing.

5. `calibration_holdout_packet_owner`
   - High accuracy ROI when rights-safe, same-family, same-basis
     measured evidence exists.
   - Ready only for a small bounded family holdout set with admissible
     basis and negative boundaries.
   - Reject broad source crawling or source-absent retuning.

6. `opening_leak_common_wall_or_open_web_residual_owner`
   - Useful residual family if local formula/anchor evidence exists.
   - Ready only with same-basis evidence or an owned residual formula.
   - Reject when accepted holdout rows are still zero or source absent.

7. `project_user_measured_anchor_next_step_owner`
   - Useful if it turns exact measured evidence into bounded future
     calculator capability, such as same-basis compatible deltas or
     field/building adapters from measured curves.
   - Ready only after it proves construction boundary and output basis.
   - Reject UI/storage-only anchor management.

8. `broad_ui_report_source_or_cleanup_work`
   - Must be rejected unless the user explicitly requests that separate
     non-calculator work.

## Selection Requirements

The selected candidate must state:

- target layer family and request shapes;
- target outputs and metric bases;
- formula, exact-source, compatible-anchor, or adapter route;
- route-required physical inputs;
- new calculable request shapes and target outputs estimated;
- basis promotions and runtime values expected to move;
- unsupported/needs-input boundaries that protect adjacent metrics.

Prefer the candidate with the highest credible calculator ROI and the
lowest aliasing risk. If no runtime owner is ready, select a bounded
no-runtime prerequisite only when it directly unlocks a calculator
route, input surface, metric basis, or evidence boundary.

## Expected Counters

The rerank itself should move no runtime values:

- `candidateCount >= 6`;
- `roiAnalysisIterations >= 4`;
- `runtimeValuesMoved 0`;
- `runtimeFormulaRetunes: 0`;
- `sourceRowsImported: 0`;
- `frontendImplementationFilesTouched: 0`.

It should estimate the next selected owner in terms of:

- `estimatedNextCalculableRequestShapes`;
- `estimatedNextCalculableTargetOutputs`;
- `estimatedNextRuntimeBasisPromotions`;
- `estimatedNextRuntimeValuesMoved`;
- `estimatedNextRequiredPhysicalInputsCaptured`;
- `estimatedNextUnsupportedBoundariesProtected`.

## Validation

Run the rerank contract once implemented:

```bash
pnpm --filter @dynecho/engine exec vitest run src/post-v1-project-user-measured-wall-airborne-frequency-exact-curve-bridge-coverage-refresh-contract.test.ts src/post-v1-runtime-first-route-family-rerank-after-project-user-measured-wall-airborne-frequency-exact-curve-bridge-coverage-refresh-contract.test.ts --maxWorkers=1
git diff --check
```

Run broader current gate only if the rerank edits runtime files,
shared schemas, estimate plumbing, route resolver behavior, or current
gate membership.
