# Checkpoint - 2026-05-04 - Company Internal Frequent Combination Lane Snapshot Guard Gate A

Slice id: `company_internal_frequent_combination_lane_snapshot_guard_v1`

Status: LANDED / NO RUNTIME

Landed gate:

`gate_a_pin_company_internal_frequent_combination_lane_snapshot_matrix_no_runtime`

Selection status:

`company_internal_frequent_combination_snapshot_matrix_landed_no_runtime_selected_visible_gate_b`

Selected next file:

`apps/web/features/workbench/company-internal-frequent-combination-lane-snapshot-guard-gate-b-visible.test.ts`

Selected next action:

`gate_b_add_company_internal_visible_route_output_snapshot_guard_no_runtime`

## What Landed

Gate A added the executable
`company_internal_frequent_combination_snapshot_matrix` at
`packages/engine/src/company-internal-frequent-combination-lane-snapshot-guard-gate-a-contract.test.ts`.
It keeps runtime values, support, confidence, evidence, API behavior,
route-card values, output-card status, proposal/report copy, and
workbench-input behavior frozen.

Rockwool triple-leaf remains not fixed. The grouped split-rockwool /
gypsum / MLV wall is still `Rw 41`, low confidence, and
`multileaf_screening_blend`. Uris 2006 remains
`paused_waiting_rights_safe_source_packet`; the current result is
screening, not exact and not source-validated.

## Gate A Artifacts

`company_internal_frequent_combination_snapshot_matrix`

The matrix pins current family, strategy, support, confidence,
source/origin, warnings, and visible posture for common wall/floor
combinations before any company-internal high-accuracy opening.

`rockwool_triple_leaf_screening_and_flat_swap_negative_rows`

The grouped rockwool triple-leaf row stays at `Rw 41` screening. The
flat-list adjacent swap stays fail-closed at
`multileaf_screening_blend_fail_closed_until_grouped_topology` with a
bounded 1 dB delta. Neither row is a fix.

`ordinary_lsf_timber_layer_swap_snapshot_rows`

Ordinary double-leaf and simple stud rows remain protected negative
boundaries. Classic framed triple-leaf swaps stay low-confidence and
warning-backed.

`masonry_lined_massive_boundary_snapshot_rows`

AAC / board / fill / gap hybrids can still cross into
`lined_massive_wall` with `family-boundary hold`. That drift is pinned
as a risk, not smoothed or retuned into exact-looking behavior.

`floor_role_inference_duplicate_stack_snapshot_rows`

The raw open-box exact floor row remains exact only inside its owned
match envelope and still warns that arbitrary raw floor reorder
invariance is not claimed. Ambiguous raw CLT floor role inference keeps
impact outputs unsupported until roles are supplied.

`near_source_alias_and_hostile_input_negative_rows`

Generic gypsum / Type C-like / MLV-plaster alias rows remain
formula-owned context only. Unknown material and infinite-thickness
API/import payloads fail closed with no supported outputs.

`field_outputs_never_design_grade_without_owner`

Field-looking values such as `R'w` and `DnT,w` remain building
prediction / screening continuations without a design-grade owner.

`standing_lane_misclassification_monitoring_mandate`

Every future implementation pass must keep watching for wrong lane,
wrong source, false exact promotion, and field-output overclaiming in
frequently used combinations. If a suspicious result appears, use the
`note_test_document_or_easy_fix` rule: note it, test whether the output
is genuinely wrong or misleading, document it if not immediately
fixable, and apply a bounded easy fix only when the source/behavior
contract is clear.

## Next Gate

Gate B must add web-visible route/output snapshot coverage before this
guard can support a company-internal opening decision:

`apps/web/features/workbench/company-internal-frequent-combination-lane-snapshot-guard-gate-b-visible.test.ts`

Action:

`gate_b_add_company_internal_visible_route_output_snapshot_guard_no_runtime`

Gate B must verify that frequent rockwool triple-leaf, flat-list swap,
ordinary double-leaf/stud, lined-massive boundary, raw floor role
prompt, near-source alias, hostile input, and field-output continuation
states are visible as screening / fail-closed / needs-input when they
are not exact.

## Validation

Validation completed on 2026-05-04:

- focused Gate A passed 1 file / 8 tests:
  `pnpm --filter @dynecho/engine exec vitest run src/company-internal-frequent-combination-lane-snapshot-guard-gate-a-contract.test.ts --maxWorkers=1`;
- engine continuity with V21, company blocker, route/source risk,
  common-combination sentinel, flat-list multileaf guard, and floor
  raw-role guard passed 7 files / 50 tests;
- web continuity with rockwool visible rehearsal, flat-list multileaf
  route-card guard, field-output Gate B, and raw-floor hostile input
  route-card guard passed 4 files / 15 tests;
- `pnpm calculator:gate:current` passed with engine 243 files / 1404
  tests, web 50 files / 238 passed + 18 skipped, repo build 5 / 5
  tasks, and whitespace guard green.

Known non-fatal build warnings remain the existing optional
`sharp/@img` warnings through `@turbodocx/html-to-docx`. Runtime-visible
calculator behavior stayed frozen.
