# Post-V1 Next Numeric Coverage Gap After Floor User-Material Impact Context Field-Only Adapter Plan - 2026-06-15

## North Star

DynEcho is an acoustic calculator. This rerank is a bounded no-runtime
selection step: subtract the newly closed custom heavy floating-floor
field-only impact route, then choose the next highest-ROI numeric gap
that improves calculator scope, accuracy, formula ownership, required
input capture, or metric/basis boundaries.

This is not a broad source crawl, UI polish pass, material-editor task,
library cleanup, formula retune without evidence, or ASTM aliasing
shortcut.

## Previous Chain

Previous rerank action:

`post_v1_next_numeric_coverage_gap_after_floor_user_material_impact_context_dynamic_stiffness_plan`

Previous rerank file:

`packages/engine/src/post-v1-next-numeric-coverage-gap-after-floor-user-material-impact-context-dynamic-stiffness-contract.test.ts`

Previous rerank status:

`post_v1_next_numeric_coverage_gap_after_floor_user_material_impact_context_dynamic_stiffness_landed_no_runtime_selected_floor_user_material_impact_context_field_only_adapter_owner`

Previous owner action:

`post_v1_floor_user_material_impact_context_field_only_adapter_owner_plan`

Previous owner file:

`packages/engine/src/post-v1-floor-user-material-impact-context-field-only-adapter-owner-contract.test.ts`

Previous owner status:

`post_v1_floor_user_material_impact_context_field_only_adapter_owner_landed_runtime_selected_coverage_refresh`

Previous selected candidate:

`floor.user_material_impact_context_field_only_adapter_owner`

Coverage refresh action:

`post_v1_floor_user_material_impact_context_field_only_adapter_coverage_refresh_plan`

Coverage refresh file:

`packages/engine/src/post-v1-floor-user-material-impact-context-field-only-adapter-coverage-refresh-contract.test.ts`

Coverage refresh status:

`post_v1_floor_user_material_impact_context_field_only_adapter_coverage_refresh_landed_no_runtime_selected_next_numeric_coverage_gap`

## Closed Route To Subtract

The refresh re-probes custom visible heavy floating-floor field-only
impact requests without moving runtime values. With
`floorImpactContext.resilientLayerDynamicStiffnessMNm3: 30`,
`floorImpactContext.loadBasisKgM2: 76`, `impactFieldContext.fieldKDb:
2`, receiving-room volume `55 m3`, and `ci50_2500Db: 3`, the route
supports `L'n,w 52.3`, `L'nT,w 49.9`, and `L'nT,50 52.9` from the
already-owned `Ln,w 50.3` / `DeltaLw 24.3` lab anchor.

Refresh counters: `coverageRefreshContractFilesTouched: 1`,
`newCalculableLayerTemplates: 0`, `newCalculableRequestShapes: 0`,
`newCalculableTargetOutputs: 0`, `runtimeBasisPromotions: 0`,
`runtimeValuesMoved 0`, `runtimeFormulaRetunes: 0`,
`sourceRowsImported: 0`, and `frontendImplementationFilesTouched: 0`.

## Rerank Instructions

Run at least three ROI passes before selecting a runtime owner. In each
pass, remove closed lanes first:

- compatible anchor-delta wall companions and field/building adapters;
- direct-fixed double-leaf/framed lab, field, building, and A-weighted
  lanes already owned by Gate EO, Gate I, and Gate AR;
- default-catalog double-leaf/framed route-input lanes;
- explicit user-material double-leaf/framed route-input lanes;
- user-material missing-topology input-surface ownership;
- user-material porous flow-resistivity input ownership;
- floor user-material impact-context dynamic-stiffness ownership;
- mixed custom floor impact field-adapter ownership;
- custom heavy floating-floor field-only impact ownership closed by
  this refresh.

Candidate categories to evaluate next:

- missing low-frequency impact input owner for otherwise calculable
  floor field requests where only `ci50_2500Db` is absent;
- building-prediction impact outputs for floor assemblies where lab and
  field anchors are already physically owned;
- exact contour-based `IIC` / `AIIC` ownership only if the standard
  contour route and required spectra are explicitly available;
- remaining user-material wall or floor inputs that already have enough
  physical data for an owned formula but are blocked by resolver/input
  ownership;
- source/exact holdouts only when the construction and metric basis
  truly match.

Reject candidates that mainly create UI polish, source crawling,
confidence copy, broad refactoring, or metric aliasing. Do not infer
field K, room volume, CI, dynamic stiffness, load basis, topology, or
spectra from tags.

## Selected Work

Selected next action:

`post_v1_floor_user_material_low_density_floating_floor_family_owner_plan`

Selected next file:

`packages/engine/src/post-v1-floor-user-material-low-density-floating-floor-family-owner-contract.test.ts`

Selected next plan:

`docs/calculator/POST_V1_FLOOR_USER_MATERIAL_LOW_DENSITY_FLOATING_FLOOR_FAMILY_OWNER_PLAN_2026-06-15.md`

Selected next label:

`post-V1 floor user-material low-density floating-floor family owner`

Rerank action:

`post_v1_next_numeric_coverage_gap_after_floor_user_material_impact_context_field_only_adapter_plan`

Rerank file:

`packages/engine/src/post-v1-next-numeric-coverage-gap-after-floor-user-material-impact-context-field-only-adapter-contract.test.ts`

Rerank plan:

`docs/calculator/POST_V1_NEXT_NUMERIC_COVERAGE_GAP_AFTER_FLOOR_USER_MATERIAL_IMPACT_CONTEXT_FIELD_ONLY_ADAPTER_PLAN_2026-06-15.md`

Rerank status:

`post_v1_next_numeric_coverage_gap_after_floor_user_material_impact_context_field_only_adapter_landed_no_runtime_selected_floor_user_material_low_density_floating_floor_family_owner`

Selected candidate:

`floor.user_material_low_density_floating_floor_family_owner`

The rerank ran `roiAnalysisIterations: 4` across `candidateCount 9`.
It selected custom low-density user-material floor ownership because the
existing lightweight-concrete family, DeltaLw, and field-adapter routes
can move real values once the visible custom base is classified as a
low-density/lightweight concrete carrier. Estimated next movement:
`Rw 53`, `Ln,w 64.3`, `DeltaLw 24.3`, `L'n,w 66.3`, `L'nT,w 63.9`,
and `L'nT,50 66.9`. It rejected CI50 defaulting, generic `IIC`/`AIIC`
aliasing, broad source crawl, and UI cleanup.

## Validation

The next rerank contract should assert the closed refresh chain above,
record the ROI iterations and candidate count, and land with
`runtimeValuesMoved 0`, `runtimeFormulaRetunes: 0`,
`sourceRowsImported: 0`, and `frontendImplementationFilesTouched: 0`.
It should select exactly one bounded runtime/input owner or explicitly
state why no higher-ROI calculator slice remains.
