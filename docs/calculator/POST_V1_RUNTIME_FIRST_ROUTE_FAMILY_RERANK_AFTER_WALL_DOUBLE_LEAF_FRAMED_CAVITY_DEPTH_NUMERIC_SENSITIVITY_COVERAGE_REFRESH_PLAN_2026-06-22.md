# Post-V1 Runtime-First Route-Family Rerank After Wall Double-Leaf/Framed Cavity-Depth Numeric Sensitivity Coverage Refresh - 2026-06-22

## Purpose

This is the selected next no-runtime rerank after the wall
double-leaf/framed cavity-depth numeric-sensitivity coverage refresh. It
must choose the next value-moving calculator owner without drifting into
UI polish, broad source crawling, generic refactoring, or documentation
work as the goal.

The rerank exists because the previous owner/refresh chain finished a
focused physical-input sensitivity lane for the context-owned
double-leaf/framed wall route. The next implementation should again
increase calculator scope, numeric accuracy, formula-route ownership,
required input capture, or metric/basis integrity.

Selected next action:

`post_v1_runtime_first_route_family_rerank_after_wall_double_leaf_framed_cavity_depth_numeric_sensitivity_coverage_refresh_plan`

Selected next file:

`packages/engine/src/post-v1-runtime-first-route-family-rerank-after-wall-double-leaf-framed-cavity-depth-numeric-sensitivity-coverage-refresh-contract.test.ts`

Selected next label:

`post-V1 runtime-first route-family rerank after wall double-leaf/framed cavity-depth numeric sensitivity coverage refresh`

## Landing Result

Landed rerank status:

`post_v1_runtime_first_route_family_rerank_after_wall_double_leaf_framed_cavity_depth_numeric_sensitivity_coverage_refresh_landed_no_runtime_selected_leaf_surface_mass_numeric_sensitivity_owner`

Selected runtime owner candidate:

`wall.double_leaf_framed.leaf_surface_mass_numeric_sensitivity_owner`

Selected runtime owner action:

`post_v1_wall_double_leaf_framed_leaf_surface_mass_numeric_sensitivity_owner_plan`

Selected runtime owner file:

`packages/engine/src/post-v1-wall-double-leaf-framed-leaf-surface-mass-numeric-sensitivity-owner-contract.test.ts`

Selected runtime owner plan:

`docs/calculator/POST_V1_WALL_DOUBLE_LEAF_FRAMED_LEAF_SURFACE_MASS_NUMERIC_SENSITIVITY_OWNER_PLAN_2026-06-22.md`

Selected runtime owner label:

`post-V1 wall double-leaf/framed leaf surface-mass numeric sensitivity owner`

The rerank selected side-leaf surface mass because the owned Gate S /
Gate I / Gate AR double-leaf/framed route already uses side A and side
B surface mass, total leaf mass, leaf mass ratio, and mass-air-mass
resonance. This is a bounded accuracy/sensitivity owner for arbitrary
user-entered board/custom layer combinations, with no source rows and no
frontend work. Counters: `candidateCount: 8`,
`roiAnalysisIterations: 4`, `estimatedNextRuntimeValuesMoved: 13`,
`estimatedNextAccuracyPromotedTargetOutputs: 13`,
`runtimeValuesMoved 0`, `runtimeFormulaRetunes: 0`,
`sourceRowsImported: 0`, and `frontendImplementationFilesTouched: 0`.
This is not a broad source crawl.

## Previous Refresh

Previous coverage refresh:

`post_v1_wall_double_leaf_framed_cavity_depth_numeric_sensitivity_coverage_refresh_plan`

Previous coverage refresh file:

`packages/engine/src/post-v1-wall-double-leaf-framed-cavity-depth-numeric-sensitivity-coverage-refresh-contract.test.ts`

Previous coverage refresh status:

`post_v1_wall_double_leaf_framed_cavity_depth_numeric_sensitivity_coverage_refresh_landed_no_runtime_selected_runtime_first_route_family_rerank`

Selected candidate re-probed:

`wall.double_leaf_framed.cavity_depth_numeric_sensitivity_owner`

Previous owner status:

`post_v1_wall_double_leaf_framed_cavity_depth_numeric_sensitivity_owner_landed_runtime_selected_coverage_refresh`

The refresh protects the landed cavity-depth owner without moving
runtime values. It re-probes topology `cavity1DepthMm = 60`, `90`, and
`140 mm`; advanced-only `advancedWall.cavities[0].depthMm` lab, field,
and building behavior; missing, zero, and negative depth as
`needs_input`; topology-authoritative mismatch behavior; and `IIC` /
`AIIC` as `unsupported`. Counters: `coverageRefreshContractFilesTouched:
1`, `newCalculableLayerTemplates: 0`, `newCalculableRequestShapes: 0`,
`newCalculableTargetOutputs: 0`, `runtimeBasisPromotions: 0`,
`runtimeValuesMoved 0`, `runtimeFormulaRetunes: 0`,
`sourceRowsImported: 0`, and `frontendImplementationFilesTouched: 0`.
This is not a broad source crawl.

## Rerank Rules

Use the calculator answer order:

1. exact measured/source row only on true construction and metric-basis
   identity;
2. same-family anchor or bounded delta only with a proven physical
   boundary;
3. owned formula route with route-required inputs;
4. precise `needs_input` or `unsupported` when the route is not owned.

The rerank must compare candidates by expected calculator impact:

- new source-absent layer combinations opened by owned formulas;
- existing combinations made more numerically sensitive to physical
  inputs;
- new target outputs produced on their correct basis;
- missing route inputs converted to precise `needs_input`;
- measured/anchor rows bounded without aliasing metrics;
- unsupported adjacent bases kept blocked.

## Planning Iterations

Iteration 1: inventory implementation-ready candidates from the live
docs, current gate, and existing runtime contracts. Start with
user-material physical input coverage as the default high-ROI lane, then
compare building/flanking runtime, frequency-band backbone, companion
metric completeness, calibration/holdouts, opening/leak, common-wall,
open-web/open-box, and other named residual families.

Iteration 2: score each candidate for route ownership, required physical
inputs, target outputs opened, negative boundaries, available evidence,
and implementation blast radius. Reject candidates whose main output is
docs-only cleanup, source crawling, confidence text, or UI polish.

Iteration 3: write a selection card for the winning runtime owner before
implementation:

- user construction / formula family;
- target outputs to open;
- formula, measured-row, anchor, or adapter route;
- required physical inputs;
- `needs_input` behavior for missing inputs;
- `unsupported` boundaries that must remain blocked;
- expected `newCalculableRequestShapes`, `newCalculableTargetOutputs`,
  `runtimeBasisPromotions`, and `runtimeValuesMoved`.

Use internet research only when a candidate depends on a current
standard, formula, manufacturer/lab evidence, or market-tool capability
that is not already defensible from local docs and implementation.

## Implementation Steps

1. Create the selected rerank contract file and encode the candidate
   comparison, no-go boundaries, and selected next runtime owner.
2. Keep the rerank no-runtime: do not retune formulas, import source
   rows, or touch frontend files in this step.
3. If the rerank chooses a runtime owner with high confidence, create
   that owner's plan document before implementation.
4. Add the rerank contract to `tools/dev/run-calculator-current-gate.ts`.
5. Run the targeted rerank test and `pnpm calculator:gate:current`.

## Expected Rerank Counters

- `routeFamilyRerankContractFilesTouched: 1`
- `newCalculableLayerTemplates: 0`
- `newCalculableRequestShapes: 0`
- `newCalculableTargetOutputs: 0`
- `runtimeBasisPromotions: 0`
- `runtimeValuesMoved 0`
- `runtimeFormulaRetunes: 0`
- `sourceRowsImported: 0`
- `frontendImplementationFilesTouched: 0`

## Stop Conditions

- Do not implement a runtime owner until the rerank names a concrete
  formula family and selection card.
- Do not broaden into source crawling unless a chosen formula/anchor
  route requires specific evidence.
- Do not promote lab values into field/building metrics without owned
  adapter terms.
- Do not alias impact outputs such as `IIC` or `AIIC` from airborne
  wall routes.
