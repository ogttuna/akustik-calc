# Post-V1 Next Numeric Coverage Gap After Direct-Fixed Context Absorptive Cavity Field/Building Adapter Plan - 2026-06-11

## Landed Closeout

This rerank landed as
`post_v1_next_numeric_coverage_gap_after_direct_fixed_context_absorptive_cavity_field_building_adapter_plan`
in
`packages/engine/src/post-v1-next-numeric-coverage-gap-after-direct-fixed-context-absorptive-cavity-field-building-adapter-contract.test.ts`
with status
`post_v1_next_numeric_coverage_gap_after_direct_fixed_context_absorptive_cavity_field_building_adapter_landed_no_runtime_selected_direct_fixed_a_weighted_field_building_owner`.
Selected candidate:
`wall.direct_fixed_double_leaf.a_weighted_field_building_owner`.
It ran `roiAnalysisIterations: 3`, selected
`post_v1_wall_double_leaf_framed_direct_fixed_a_weighted_field_building_owner_plan`
/
`packages/engine/src/post-v1-wall-double-leaf-framed-direct-fixed-a-weighted-field-building-owner-contract.test.ts`
/
`docs/calculator/POST_V1_WALL_DOUBLE_LEAF_FRAMED_DIRECT_FIXED_A_WEIGHTED_FIELD_BUILDING_OWNER_PLAN_2026-06-11.md`
/
`post-V1 wall double-leaf/framed direct-fixed A-weighted field/building owner`,
and moved no runtime values in the selection step. It estimates
`estimatedNextRuntimeValuesMoved: 12`: four currently unsupported
full/partial absorptive A-only request shapes plus two empty
direct-fixed A-only request shapes that need Gate ER correction. This is
not a broad source crawl. Counters: `candidateCount 7`,
`aWeightedOnlyUnsupportedRowsRechecked 4`,
`aWeightedOnlyMisroutedRowsRechecked 2`,
`immediateRuntimeValuesMoved: 0`, `runtimeBasisPromotions: 0`,
`runtimeFormulaRetunes: 0`, `runtimeValuesMoved 0`,
`sourceRowsImported: 0`, and `frontendImplementationFilesTouched: 0`.

## North Star

DynEcho is an acoustic calculator. This rerank must select the next
bounded calculator improvement after the direct-fixed context-owned
absorptive cavity field/building adapter closeout. It must not select
broad source crawling, report polish, auth/storage, generic UI work, or
another closed lane.

## Previous Gate

Previous owner:

`post_v1_wall_double_leaf_framed_direct_fixed_context_absorptive_cavity_field_building_adapter_owner_plan`

Previous owner file:

`packages/engine/src/post-v1-wall-double-leaf-framed-direct-fixed-context-absorptive-cavity-field-building-adapter-owner-contract.test.ts`

Previous owner status:

`post_v1_wall_double_leaf_framed_direct_fixed_context_absorptive_cavity_field_building_adapter_owner_landed_runtime_selected_surface_parity`

Previous surface parity:

`post_v1_wall_double_leaf_framed_direct_fixed_context_absorptive_cavity_field_building_adapter_surface_parity_plan`

Previous surface parity file:

`apps/web/features/workbench/post-v1-wall-double-leaf-framed-direct-fixed-context-absorptive-cavity-field-building-adapter-surface-parity.test.ts`

Previous surface parity status:

`post_v1_wall_double_leaf_framed_direct_fixed_context_absorptive_cavity_field_building_adapter_surface_parity_landed_no_runtime_selected_coverage_refresh`

Previous selected next action, now landed:

`post_v1_wall_double_leaf_framed_direct_fixed_context_absorptive_cavity_field_building_adapter_coverage_refresh_plan`

Previous file:

`packages/engine/src/post-v1-wall-double-leaf-framed-direct-fixed-context-absorptive-cavity-field-building-adapter-coverage-refresh-contract.test.ts`

Previous status:

`post_v1_wall_double_leaf_framed_direct_fixed_context_absorptive_cavity_field_building_adapter_coverage_refresh_landed_no_runtime_selected_next_numeric_coverage_gap`

The previous refresh froze the direct-fixed context-owned absorptive
cavity field/building adapter through the live runtime route and the
resolver coverage posture. Field requests use
`gate_i_airborne_field_apparent_context_adapter_runtime`; building
requests use
`gate_ar_airborne_building_prediction_all_owner_runtime_corridor`; both
use `wall.direct_fixed_double_leaf.equivalent_coupled_mass_bridge_loss_owner`
as the Gate EO base curve. Closed values are full absorptive
field/building `R'w 29`, `Dn,w 30`, `DnT,w 32` and partial absorptive
field/building `R'w 27`, `Dn,w 28`, `DnT,w 30`. The registry, adapter,
surface, coverage matrix, and company-internal V0 keep the Gate I / Gate
AR adapter candidates visible as `ready_with_budget` and
`allowed_with_budget`.

Previous refresh counters: `coverageRefreshContractFilesTouched: 1`,
`webSurfaceParityContractFilesTouched: 1`,
`frontendImplementationFilesTouched: 0`, `runtimeValuesMoved 0`,
`runtimeFormulaRetunes: 0`, and `sourceRowsImported: 0`.

## Iteration 1 - Subtract Closed Lanes

Closed lanes must not be reselected:

- context-only empty-cavity double-leaf/framed lab `Rw`, `STC`, `C`, and
  `Ctr`;
- context-owned non-direct absorptive double-leaf/framed lab `Rw`, `STC`,
  `C`, and `Ctr`;
- direct-fixed context-owned absorptive cavity lab `Rw`, `STC`, `C`, and
  `Ctr`;
- direct-fixed empty field/building `R'w`, `Dn,w`, and `DnT,w`;
- direct-fixed context-owned absorptive cavity field/building `R'w`,
  `Dn,w`, and `DnT,w`;
- surface parity and coverage-only passes for the above routes.

This removes another surface, report, or coverage-only pass from the
immediate candidate set.

## Iteration 2 - Candidate Comparison Rules

The rerank must compare calculator-scope candidates that can plausibly
move numeric coverage or accuracy:

- direct-fixed context-owned absorptive cavity A-weighted field/building
  ownership if the Gate EO curve and Gate I / Gate AR adapters already
  expose enough owned curve terms to calculate `Dn,A` and `DnT,A`
  without aliasing;
- direct-fixed context-owned absorptive cavity budget tightening only if
  same-basis measured holdouts exist;
- non-direct double-leaf/framed formula widening only when required
  physical inputs and family boundaries are already owned;
- additional route-input surface work only if it unlocks more dynamic
  formula coverage than the current closed direct-fixed lane;
- any broad source crawl, report UI, auth/storage, or finite scenario
  pack stays lower-ranked unless it directly supplies named formula
  evidence for the selected route.

The rerank must preserve A-weighted-only, ASTM/IIC/AIIC, impact, missing
absorber ownership, Gate AY panellized input, non-direct-fixed absorber
stack, exact-source precedence, and thick-board Auto boundaries while
scoring candidates.

## Selected Work

Selected action:

`post_v1_next_numeric_coverage_gap_after_direct_fixed_context_absorptive_cavity_field_building_adapter_plan`

Selected file:

`packages/engine/src/post-v1-next-numeric-coverage-gap-after-direct-fixed-context-absorptive-cavity-field-building-adapter-contract.test.ts`

Selected label:

`post-V1 next numeric coverage gap after direct-fixed context absorptive cavity field/building adapter`

Selected plan doc:

`docs/calculator/POST_V1_NEXT_NUMERIC_COVERAGE_GAP_AFTER_DIRECT_FIXED_CONTEXT_ABSORPTIVE_CAVITY_FIELD_BUILDING_ADAPTER_PLAN_2026-06-11.md`

This selected work is a no-runtime rerank. It must perform at least
three ROI iterations, subtract closed lanes, evaluate candidate scope and
accuracy impact, and then select the next bounded value-moving or
accuracy-moving calculator slice. It should import no source rows, retune
no formulas, and touch no frontend implementation in the selection step.

## Expected Counters

Counters for the selected rerank should include:

- `candidateCount`;
- `roiAnalysisIterations`;
- `closedDirectFixedFieldBuildingRowsRechecked`;
- `estimatedNextRuntimeValuesMoved` or `estimatedNextFormulaAccuracyMovement`;
- `immediateRuntimeValuesMoved: 0`;
- `runtimeBasisPromotions: 0`;
- `runtimeFormulaRetunes: 0`;
- `sourceRowsImported: 0`;
- `frontendImplementationFilesTouched: 0`.

This is not a broad source crawl.
