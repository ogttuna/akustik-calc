# DynEcho Handoff — 2026-04-04

This note captures where the dynamic-calculator hardening pass stopped and what the next implementer should do first.

Read together with:

- `docs/archive/status/STATUS_2026-04-04.md`
- `docs/calculator/CURRENT_STATE.md`
- `docs/calculator/SOURCE_GAP_LEDGER.md`
- `docs/calculator/DYNAMIC_CALCULATOR_PLAN.md`

## 1. What Landed

### 1.1 Stability Hardening

Completed:

- canonicalized localized number parsing
- repaired reset state so it no longer jumps across floor/wall study modes
- narrowed reorder canonicalization to the defended simple heavy-floor package
- aligned section preview and technical schedule to solver-layer order

Practical effect:

- the same final floor package is less likely to drift numerically because of edit path, reorder noise, or locale parsing

### 1.2 Source-Backed Floor Widening

Completed:

- UBIQ open-web widening through the defended `FL-24 -> FL-26 -> FL-28` corridor
- TUAS open-box widening through the defended `a/b` corridor
- TUAS `R2b` import after drawing audit confirmed it matches the current `b` visible-layer shorthand directly

Practical effect:

- generic `resilient_stud_ceiling` basic open-box route now lands on `R2b`
- explicit `tuas_open_box_ceiling_family_a` still lands on `R2a`
- source-backed widening decisions are now better aligned with the visible-layer truth surface

### 1.3 Living Documentation

Completed:

- current calculator posture is recorded in the living docs
- source-gap and widening decisions are tracked in ledger form
- the remaining TUAS shortlist is now explicitly described as branch-design work, not just import backlog

## 2. Important Current Truths

- do not treat generic `resilient_stud_ceiling` as ambiguous between `R2a` and `R2b`
- do not import `R7a` as if it were just another dry `family_a` sibling
- do not import `R6b` as if it were just another two-board `family_b` sibling

Current interpretation from the TUAS construction drawings:

- `R7a`
  - likely separate heavy/wet `family_a` branch
  - top package includes `50 mm EPS + 40 mm screed`
- `R6b`
  - likely separate reinforced lower-treatment `family_b` branch
  - lower side uses four gypsum boards rather than the current two-board shorthand

## 3. First Next Step

Run the broader complex-stack torture pass before widening anything else.

That pass should include:

- deeper floor stacks
- deeper wall stacks
- reorder, split, duplicate, reset, and partial-edit cycles
- browser-driven checks on the built app, not only regression tests

## 4. Second Next Step

After the torture pass, do a mini branch-design audit for:

- `R7a`
- `R6b`

Deliverable of that audit:

- either one small honest new branch surface
- or an explicit decision to keep the row deferred

## 5. Verification Used To Close This Pass

Passed before closing:

- `pnpm exec vitest run packages/engine/src/impact-predictor-input.test.ts packages/engine/src/predictor-published-family-estimate.test.ts packages/engine/src/floor-widening-candidate-contract.test.ts packages/engine/src/floor-source-corpus-contract.test.ts packages/engine/src/tuas-candidate-backlog-contract.test.ts packages/engine/src/tuas-post-corridor-screening-contract.test.ts packages/engine/src/calculate-impact-only.test.ts packages/engine/src/calculate-assembly.test.ts`
- `pnpm exec vitest run apps/web/features/workbench/floor-family-regressions.test.ts apps/web/features/workbench/scenario-analysis.test.ts apps/web/features/workbench/complex-stack-audit.test.ts`
- `pnpm -C apps/web build`
