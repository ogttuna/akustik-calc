# DynEcho Status Snapshot — 2026-04-04

Historical note:

- this snapshot is retained for chronology
- prefer the living docs under `docs/calculator` and `docs/foundation` for the current execution contract

Use it alongside:

- `docs/archive/handoffs/HANDOFF_2026-04-04.md`
- `docs/calculator/CURRENT_STATE.md`
- `docs/calculator/SOURCE_GAP_LEDGER.md`

## 1. End-Of-Day Product State

The dynamic calculator is materially more deterministic than it was at the start of the hardening pass.

Most important current gains:

- localized numeric parsing is now canonicalized across thickness, density, and dynamic stiffness inputs
- reset no longer silently flips study mode
- the simple heavy-floor lane no longer drifts when the same final package is built through different edit paths
- section preview and schedule are aligned to solver layers instead of raw visible row order
- living floor-family coverage is now tracked through source-backed ledgers and contract tests instead of ad hoc assumptions

## 2. Source-Backed Coverage State

Current defended floor-family posture:

- TUAS open-box exact corridor now includes:
  - `R2a`
  - `R2b`
  - `R3a`
  - `R3b`
  - `R5a`
  - `R5b`
  - `R11b`
- TUAS `family_a` vs `family_b` suspended-ceiling split is explicit on both predictor input and visible-layer surfaces
- UBIQ open-web corridor now has a defended `FL-24 -> FL-26 -> FL-28` exact path with the corresponding `FL-28 (FRL/D)` bound sibling

Important nuance:

- generic `resilient_stud_ceiling` is now intentionally the `TUAS b-family` shorthand
- explicit `tuas_open_box_ceiling_family_a` remains required for the `family_a` exact lane

## 3. What Is Still Open

Two kinds of open work remain and they should not be mixed together:

- stability work
  - broader complex-stack torture audit across floor and wall edit paths
- coverage work
  - TUAS `R7a` branch-design audit
  - TUAS `R6b` branch-design audit
  - later UBIQ source-trace cleanup around `ubiq_fl32_*` / `ubiq_fl33_*`

Current judgement:

- `R7a` looks like a heavier wet `family_a` branch, not a trivial corridor sibling
- `R6b` looks like a reinforced lower-treatment `family_b` branch, not a trivial corridor sibling

Neither should be imported blindly.

## 4. Recommended Next Move

Do this in order:

1. run the broader floor/wall torture pass
2. audit `R7a` and `R6b` as branch-design questions
3. only then choose the next widening move

This keeps the calculator honest:

- stable where it already claims support
- explicit where topology still needs new branch structure

## 5. Closing Verification

End-of-day checks that passed:

- targeted engine vitest pack for TUAS/UBIQ widening, predictor routing, and assembly calculations
- targeted workbench vitest pack for family regressions, scenario analysis, and complex-stack audit
- `pnpm -C apps/web build`
